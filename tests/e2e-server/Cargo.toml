[package]
name = "e2e-server"
version = "0.1.0"
edition = "2021"
description = "E2E test: raw TCP client (26.2) against the Neutron server"

[workspace]

[[bin]]
name = "e2e-test"
path = "main.rs"

[dependencies]
bytes = "1"
uuid = { version = "1", features = ["v4"] }
neutron-protocol = { path = "../../crates/neutron-protocol" }
