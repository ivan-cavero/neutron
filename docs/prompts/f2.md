# F2 — Worldgen parity 1:1 — LEAD prompt

> Paste into pi. Read `ARCHITECTURE.md` §6 (Worldgen) fully, `STATE.md`, `runs/README.md`.

## Objective

Same seed → same world, verified by checksum.

## Bar

- [ ] xxHash64 identical to vanilla on 50 golden seeds (0 mismatches)
- [ ] cps > 250 @16 threads, reproduced on Windows and Linux
- [ ] World generated by Neutron opens in vanilla with the same terrain

## Tasks

- T1 — Golden data: 50 seeds, xxHash64 per chunk, `hashes.json`. `tools/golden-data/`.
- T2 — Noise + density functions + biomes: exact XORoshiro128 PRNG, 3D Perlin octaves,
  density functions from vanilla datapack JSON, biome source (6 params).
- T3 — Surface rules + carvers + features (depends T2).
- T4 — Structures phase 1 (depends T3).
- T5 — CPS benchmark (depends T1).

Each task: What / AC (+ ratchet) / Evidence / DoD.

## Gauntlet Loop

Builders via `subagent` (T1+T2 parallel, then chain). Blind critic per component
(golden checksums, parity tests, benchmark). FAIL → biggest gap → repeat.

## Output

- `tools/golden-data/` with 50+ reference seeds
- `crates/neutron-worldgen/` with 1:1 parity
- `tests/benchmarks/results/cps-f2.json`, `runs/run-NNN.md`, updated `STATE.md`
