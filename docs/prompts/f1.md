# F1 — Playable core — LEAD prompt

> Paste into pi. Read `STATE.md`, `runs/README.md`, `AGENTS.md`, `ARCHITECTURE.md` §3 (Protocol) and §5 (World).

## Objective

A real player joins, plays, and the world persists in vanilla Anvil.

## Bar

- [ ] Bot vanilla 26.2 plays 10 min without kick (E2E in CI)
- [ ] Saved world opens in vanilla and vice versa
- [ ] Fuzz of decode: 1M inputs, no panic
- [ ] Startup < 2 s; RAM < 200 MB

## Tasks

- T1 — Protocol 26.2: login/play packets (handshake, login, keep_alive, join_game, chat,
  position, spawn, chunk_data, block_update). `crates/neutron-protocol/`.
- T2 — World Anvil: `.mca` read/write, `level.dat`, vanilla folder structure,
  `session.lock`. `crates/neutron-world/`.
- T3 — Fuzz decode: cargo-fuzz, 1M inputs, 0 panics, JSON report.
- T4 — E2E: bot 26.2 connects, plays 10 min no kick; measure startup <2s, RAM <200MB.

Each task: What / AC (+ ratchet: no regression on existing tests) / Evidence / DoD.

## Gauntlet Loop

Builders via `subagent` (parallel). Blind critic per piece (clean context, real evidence:
packet flow vs wiki.vg, world opens in vanilla, fuzz report, E2E logs). FAIL → biggest
gap → fix → repeat. PASS only with blind-critic evidence.

## Output

- `crates/neutron-protocol/` + `crates/neutron-world/` functional
- `bench/results/fuzz-v1.json`, `bench/results/startup-v1.json`
- `runs/run-NNN.md`, updated `STATE.md`
