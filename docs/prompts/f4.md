# F4 — Scale 500-1000+ — LEAD prompt

> Paste into pi. Read `ARCHITECTURE.md` §7 (ECS + regional scheduler) and §4 (Scale), `BENCHMARKS.md`, `STATE.md`.

## Objective

500 stable players; path to 1000+.

## Bar

- [ ] 500 bots × 60 min → TPS 20.0, p99 tick < 25 ms
- [ ] RAM/player < 1 MB over base < 150 MB
- [ ] No regression of cps/startup vs F3

## Tasks

- T1 — Regional scheduler (Folia-style): region fan-out to threads, lock-free with arenas
- T2 — Hot path optimizations (depends T1)
- T3 — Stress test 500 bots 60 min (depends T2)
- T4 — Memory profiling (dhat/heaptrack, RSS per player)

Each task: What / AC (+ ratchet) / Evidence / DoD.

## Gauntlet Loop

T1+T4 parallel, then T2, then T3. Blind critic runs the stress test, measures TPS every 5s,
reports p99, memory profile. FAIL → biggest gap → repeat.

## Output

- `crates/neutron-sim/scheduler/` functional
- `tests/benchmarks/results/stress-f4.json`, `runs/run-NNN.md`, updated `STATE.md`
