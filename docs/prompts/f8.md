# F8 — Release 1.0 — LEAD prompt

> Paste into pi. Read `STATE.md`, `ROADMAP.md`, all recent `runs/`. Ensure F0-F7 are complete.

## Objective

Stable, verifiable, defensible release.

## Bar

- [ ] Parity suite 100% green on `main`
- [ ] Benchmarks reproducible on 2 machines
- [ ] 72h uptime, 100 players, 0 memory leaks
- [ ] Fuzz 24h — 0 panics, 0 crashes
- [ ] Binaries: x86-64 + ARM64 for Win/Linux/Mac
- [ ] Docs complete

## Tasks (all parallel)

- T1 — Fuzz 24h + security audit (cargo-fuzz decode, cargo audit, unsafe review)
- T2 — Final benchmarks (2 machines, same seed, same methodology)
- T3 — Uptime test 72h with 100 bot players (memory leak, sustained TPS)
- T4 — Multi-platform binaries + GitHub release workflow
- T5 — Docs: final README, vanilla→Neutron migration guide, deployment guide,
  API reference, CHANGELOG

## Final human gate

`ask_user_question`: RELEASE GATE — parity 100%? benchmarks in targets? 72h uptime?
24h fuzz clean? binaries released? docs complete? If APPROVED: `git tag v1.0.0 && git push origin v1.0.0`.

## Output

- `main` with parity suite green, binaries in GitHub Releases (v1.0.0)
- `docs/` complete, `CHANGELOG.md`, updated `STATE.md`
