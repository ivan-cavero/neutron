# F3 — Vanilla simulation — LEAD prompt

> Paste into pi. Read `ARCHITECTURE.md` §7 (Simulation) fully, `STATE.md`, `runs/README.md`.

## Objective

Blocks, fluids, lighting, redstone, spawns, survival — vanilla behavior.

## Bar

- [ ] Positional golden suite 100% vs a real vanilla server (bots)
- [ ] Light arrays xxHash64 identical on 50 seeds
- [ ] Basic survival playable by bot
- [ ] Redstone: PP (W,E,N,S,D,U) and NC (W,E,D,U,N,S) update order exact; QC as vanilla

## Tasks (phases)

- FASE A (parallel): T1 lighting (Starlight-style) · T2 redstone A (wire, torches, levers,
  doors) · T3 fluids (water, lava, bubble columns, waterlogging) · T4 spawns (light <7,
  categories, pack spawning, despawn 32-128, caps)
- FASE B (after A): T5 redstone B (comparators, repeaters, observers, hoppers, TNT)
- FASE C (after B): T6 redstone C (pistons, QC, block swapping) — **most complex mechanic**
- FASE D: T7 positional golden suite · T8 basic survival (hunger, health, crafting,
  inventory, XP)

Each task: What / AC (+ ratchet) / Evidence / DoD. Human gate between phases
(`ask_user_question`).

## Gauntlet Loop

Builders via `subagent`. Blind critic per subsystem (run code, compare vs vanilla real
server, byte-level light arrays). FAIL → biggest gap → repeat.

## Output

- `crates/neutron-sim/` with all subsystems
- `tests/benchmarks/results/golden-f3.json`, `runs/run-NNN.md`, updated `STATE.md`
