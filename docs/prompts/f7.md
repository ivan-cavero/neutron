# F7 — Bedrock — LEAD prompt

> Paste into pi. Read `ARCHITECTURE.md` §3 (Protocol — Bedrock: independent session layer, RakNet), `STATE.md`.

## Objective

Bedrock clients in the same world.

## Bar

- [ ] Real Bedrock client plays 10 min without crashes
- [ ] Coexistence: Java + Bedrock in the same world
- [ ] No Java TPS impact with Bedrock connected

## Tasks (chain: each depends on the previous)

- T1 — RakNet + login/play: open_connection, encapsulation, Login → PlayStatus,
  Play packets (level_chunk, move_player, inventory_content, chat, set_time)
- T2 — Registry mapping Java↔Bedrock (block ids, item ids, biome ids)
- T3 — Basic play: movement, chat, blocks, inventory
- T4 — Coexistence: same world, same tick, no TPS impact

Each task: What / AC (+ ratchet) / Evidence / DoD. Human gate after T1.

## Gauntlet Loop

Chain execution. Blind critic: real Bedrock client login/spawn/move; Java+Bedrock same
world; TPS with both connected. FAIL → biggest gap → repeat.

## Output

- `crates/neutron-protocol-bedrock/` functional
- Real Bedrock client connected (screenshot + logs)
- `bench/results/coexist-f7.json`, `runs/run-NNN.md`, updated `STATE.md`
