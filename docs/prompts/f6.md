# F6 — WASM + Lua plugins — LEAD prompt

> Paste into pi. Read `ARCHITECTURE.md` §9 (Plugins) + §1 (Security by construction), `STATE.md`.

## Objective

Secure-by-construction plugin ecosystem.

## Bar (security is the priority)

- [ ] Plugin WASM panic → server survives, only the plugin dies
- [ ] Fuel 10M opcodes → plugin terminated, server intact
- [ ] Memory limit → plugin dies, server intact
- [ ] Hot reload: swap .wasm without restart
- [ ] 3 real Bukkit plugins converted
- [ ] Hot path cost < 5 µs/tick

## Tasks

- T1 — WASM runtime: wasmtime + WIT, sandbox (fuel + memory), plugin API
  (init/tick/event/command), host API (set_block/get_block/send_message), panic
  isolation, hot reload
- T2 — Lua scripting: mlua, before/after tick hooks, block/player events, coroutine
  timeout, same host API
- T3 — Bukkit→WASM converter (parse plugin.yml, transform listeners → WIT functions)
- T4 — PatchBukkit v0 (event system, command system, partial scheduler)
- T5 — Plugin docs (tutorials, API reference, security guide)

Each task: What / AC (+ ratchet) / Evidence / DoD.

## Gauntlet Loop

T1+T2 parallel; then T3+T4+T5. Blind critic: sandbox tests, fuel kill, hot reload,
criterion <5µs/tick. FAIL → biggest gap → repeat.

## Output

- `crates/neutron-plugin/` (WASM + Lua), `tools/patch-bukkit/`, 3 converted plugins
- `docs/plugins/`, `runs/run-NNN.md`, updated `STATE.md`
