//! Build-unblocking stub: the [[example]] stanza exists in Cargo.toml but
//! this file was never created, which fails manifest parsing for every
//! cargo command on this crate. Replace or delete when the real example lands.
fn main() {}
