// carver_pre_diff — CLOSED DUMP (stub replaced by session work; see
// examples/carver_edge_dump.rs for the final-block classification and
// examples/aquifer_water_diff.rs for the pre-feature water classes).
fn main() {
    eprintln!("see carver_edge_dump / aquifer_water_diff; this target is a placeholder");
}
