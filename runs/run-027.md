# Run 027 — would_survive on selector + andesite_upper diagnosis

> **Fecha**: 14 ago 2026  
> **Bar 1:1**: 100% block match — **aún no**

## 1. Trees

`RandomSelector` apunta a **placed** features (`dark_oak_leaf_litter`). Neutron cargaba el **configured** homónimo y se saltaba `would_survive` (sapling / BlockTags.DIRT).

Fix: resolver **placed primero** y aplicar filtros en el origen ya elegido.

## 2. Andesite extra (diagnóstico, sin heurística)

| Hecho | Valor |
|-------|--------|
| extra / miss | **545 / 0** |
| Y extra | **92..100** (un blob) |
| vanilla andesite Y | **solo 16..63** (solo `ore_andesite_lower`) |
| origen | (6,-2) rarity pass @ **(105, 98, -26)** |
| RNG vs 26.2 jar | **bit-exact** (rarity, xz, y, angle, sin/cos, nextDouble) |
| Java `doPlace` math | 669 unique / **634 in-chunk** |
| superficie | grass Y=135 van=neu |

Vanilla **no colocó** `andesite_upper` en este chunk pese a RNG y terreno idénticos. El gate `startY <= getHeight(OCEAN_FLOOR_WG)` debería pasar (91 ≤ ~136). No se apagó el feature (sería heurística). Pendiente: `ensureCanWrite` / heightmap real en un server 26.2.

## 3. Métricas (6,-2) seed 12345

| Métrica | R26 | **R27** |
|---------|----:|--------:|
| ALL block match | 96.96% | **97.02%** |
| BASE (no veg) | 99.00% | **99.00%** |
| extra dark_oak_leaves | 516 | **469** |
| andesite neu | 1969 | 1969 (van 1424) |

```
ALL blocks:    match 95372/98304 (97.02%)
BASE (no veg): match 94863/95818 (99.00%)
```

## 4. Tests

`cargo test -p neutron-worldgen --lib` → 40 passed.

## Resultado

FAIL (bar 1:1). Trees: filtro vanilla real. Andesite_upper: gap localizado, sin fix 1:1 todavía.

## Gaps a R28

1. Por qué vanilla no escribe el blob andesite_upper (634 bloques) con el mismo RNG
2. TreeFeature residual (~469 hojas + logs)
3. ChargeCursor
