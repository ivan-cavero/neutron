# Run 042 — Freeze worldgen + servidor jugable

> **Fecha**: 14 ago 2026  
> **Bar 1:1**: no se toca. Este run **congela** F2d R41 y conecta el generador al binario.

## Objetivo

Dejar el punto de worldgen escrito, organizar el crate, y que
`cargo run --release -p neutron-server -- --seed 12345` deje entrar a un
cliente vanilla 26.2 y ver el terreno.

## Qué se hizo

1. **Freeze** — `crates/neutron-worldgen/WORLDGEN.md` (métricas R41 + gaps).
2. **Organización** — `examples/README.md`; `chunk_sender` ya no finge superflat.
3. **Protocolo 26.2** — IDs de `packets.json` (776). Fase Configuration
   (known packs `minecraft:core` 26.2, registries sin NBT, tags del jar).
4. **Block states** — tabla de `blocks.json` 26.2 (grass=9, bedrock=85, …).
5. **Worldgen thread** — un `ChunkGenerator` + cache. Spawn = heightmap (0,0)+1.
6. **Join** — radio inicial 2; el tick stremea el resto al writer del jugador.

## Cómo entrar

```bash
cargo run --release -p neutron-server -- --seed 12345 --view-distance 8
```

Cliente 26.2 → `localhost:25565`. Creative + vuelo. Chunk bar ≈ x=96, z=-32.

## Resultado

Código listo para join. El 1:1 **sigue en 97.84 %**. Gaps en WORLDGEN.md.
