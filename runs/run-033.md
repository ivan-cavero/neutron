# Run 033 — Primer parche sculk: catalyst roll y start (103,-26,-31)

> **Fecha**: 14 ago 2026  
> **Bar 1:1**: 100% block match — **aún no**

## 1. Qué se midió (mundo real, seed 12345, chunk (6,-2))

Vanilla (palette del `world/` actual):

| Célula | Bloque vanilla |
|--------|----------------|
| (98,-43,-23) | `sculk_catalyst` |
| (103,-26,-31) | `sculk_catalyst` |
| (111,-36,-31) | `sculk` (no catalyst) |

Solo el origen `(96,-32)` puede hacer `in_square` a x=103 z=-31.

Neutron, stream intercalado (`run_patch` entre intentos, `NEUTRON_SCULK_PATCHES=1`):

```
att o=(96,-32) i=0 (98,-43,-23) biome=1 here=Air
nbhd 3×3×3 = air + deepslate (1:1 con vanilla no-sculk)
catalyst_roll=0.701103 <= 0.5 ? false
draws=2937
first_patch_r16 sculk=121 vein=76 sensor=0 shrieker=0 cat=0
```

Los 256 intentos intercalados del origen centro **nunca** producen `(103,-26,-31)` ni `(111,-36,-31)`.

`MultifaceGrowthFeature` 26.2 (javap): el bucle `searchRange` hace `setWithOffset(origin, dir)` cada iteración — **no acumula**. Las 8 venas globales no son un walk de 20 bloques roto.

`updateDecayDelay`: `Sculk`/`Vein` = siempre 1 (default interfaz); `DEFAULT` (aire) decrementa. Eso ya lo teníamos.

## 2. Conclusión

El intento 0 cae en la misma célula que el primer catalyst vanilla → `setFeatureSeed` + `in_square` + height son 1:1 **hasta el primer `run_patch`**.

Vanilla coloca catalyst ahí (`nextFloat <= 0.5` y suelo sólido). Nosotros sacamos **0.701** después de 2937 `next(bits)` de ChargeCursor. El stream ya diverge **dentro del primer parche**.

Eso desincroniza los 255 `in_square` siguientes: el segundo catalyst vanilla en la losa Y=-32 no existe como start, y faltan ~143 sculk en Y=-32..-17.

Terreno 3×3×3 pre-parche = vanilla. No es bioma ni density. Es ChargeCursor (éxito/fallo de `attemptPlaceSculk` / vida del cursor / `nextInt` de decay vs growth).

Primer parche: 121 sculk, 0 growths. Cursors parecen quedarse en venas (`hasSubstrateAccess`) y no sentarse en sculk con aire encima → no entran en `SculkBlock.attemptUseCharge` (nextInt 5/10/50/11).

## 3. Métricas

Sin cambio de volumen (solo instrumentación: `FeatureRandom::draw_count`, dumps `NEUTRON_SCULK_PATCHES`).

```
tries=2304 biome_ok=379 spread_ok=11 placed_ops=1267
final_sculk=330 vein=175 catalyst=0
```

`cargo test -p neutron-worldgen --lib --release` → **46 passed**.

## Resultado

FAIL (bar 1:1). Gap localizado: RNG de ChargeCursor del parche i=0 en (98,-43,-23).

## Gaps a R34

1. Paridad de `nextInt` del primer `run_patch` (éxito `attemptPlaceSculk` vs decay; movimiento a sculk vs vena). Si el roll del catalyst baja a ≤0.5, el intento que da (103,-26,-31) debería aparecer.
2. Dark oak (8,136,8) / 240 leaves extra
3. BASE residual
