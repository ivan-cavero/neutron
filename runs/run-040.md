# Run 040 — postProcess generateBox (coords locales) + placeSupport

> **Fecha**: 14 ago 2026  
> **Bar 1:1**: 100% block match — **aún no**

## 1. Fix

El fill del BB entero (R39) abría suelo y techo que vanilla no toca.

`placeBlock` / `generateBox` usan coords **locales** + `getWorldX/Y/Z` (orientación). Room y Crossing tienen `orientation == null` → coords mundo.

| Pieza | Aire vanilla |
|--|--|
| Room | `(minX, minY+1, minZ) .. (maxX, min(minY+3,maxY), maxZ)` + huecos de entrada |
| Corridor | local `(0,0,0)..(2,1,5*n-1)` + `generateMaybeBox` 0.8 en y=2 |
| Crossing | cruz: `minX+1..maxX-1` y `minZ+1..maxZ-1` |
| Stairs | 2 cajas + bucle de 5 escalones (javap) |

Corridor: `placeSupport` (fences + vigas) si `isSupportingBox`; `setPlanksBlock` en y=-1 (agujeros del suelo).

`canBeReplaced`: todo menos tablones/fence/tronco.

## 2. Métricas

Primer parche `(98,-43,-23)`:

| | roll | draws |
|--|--:|--:|
| R38 árbol mal | 0.996 | 5650 |
| R39 fill BB | 0.112 | 6016 |
| **R40 generateBox** | **0.029** | 5652 |

`0.029 ≤ 0.5` → catalyst en el origen.

```
ALL:  95626/98304 (97.28%)
BASE: 94845/95564 (99.25%)
```

cave_air que falta: (5,-2) **6**, (5,-1) **132** (era 20+234).

Árbol (4,-1): 121 piezas, 116/121 BB, primeras 8 bit-exact. Tests mineshaft **ok**.

## Resultado

FAIL (bar 1:1). El catalyst del primer parche **sí**. ALL baja por `postProcess` incompleto (raíles, cobweb, half-sphere, 5 BB X+3) y sculk que se desvía.

## Gaps a R41

1. 5 corredores X+3 vs NBT
2. `generateUpperHalfSphere`, raíles, cobweb, `isInInvalidLocation`
3. Recuperar ALL (98.48% pre-shaft → 97.28%)
