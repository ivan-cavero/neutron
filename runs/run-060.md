# run-060 — Splits estructurales, data-drive de step-6 y diagnóstico pale-garden

> 22 Aug 2026. Todo en `main` (último: `f3d7a85`). Paridad anclada:
> `region_parity` seed 12345 (6,-2) r=1 = **97.84%** constante en TODOS los
> refactors. `cargo test -p neutron-worldgen` = 65+1 verde.

## Hecho

1. **Splits** (sin cambio de comportamiento): `mineshaft/{mod,pieces,place}`,
   `tree/{mod,cfg,trunk_placers,foliage_placers,decorators}`,
   `sculk/{mod,place,cursor,blocks,gates,probes}`, `density/{mod,json}`,
   `feature_ports/{mod,simple,lake,sequence,underground,ice}`,
   `feature_dispatch/{mod,sampling,predicates,vegetation,fluids}`.
   Máx archivo del crate: `features.rs` 1294 (antes `feature_dispatch.rs` 2205).
2. **Paso 4 data-driven**: `load_overworld_ores()` parsea placed+configured JSON
   → mismos `FeatureDef` (equivalencia 34/34 contra la tabla a mano antes de
   borrarla). `datapack_data.rs` macro única lista (EMBEDDED_PATHS + concat!).
3. **Fixes reales**: slots de caché Cache2D/CacheOnce por-nodo (latente, blindado);
   `extra_rare_growths` IntProvider muestreado en el punto vanilla (ConstantInt no
   consume draws); biomas almacenados **4×4×4 reales** (protocolo cliente correcto,
   antes repetía el cuart medio); unión de decoración sobre todos los Y-quarts.
4. **Pines dorados**: xxh3 de ImprovedNoise/NormalNoise/BlendedNoise.
5. **Limpieza**: env-flags OnceLock, loads `&'static Value`, dead code fuera.

## Hallazgo clave — P1 era falso positivo

Intentar `is_face_sturdy_at` en el wrap-around del spreader bajó el probe Java
de flat-floor de 166→162 sculk. `BlockState.isFaceSturdy` ES el atajo
collision-shape-full-block: vena/sensor/shrieker NO son sturdy en ninguna cara.
El blacklist-check original ya era vanilla-correcto. Revertido con nota doc
citando los números (`a60972a`). Lección: ni los análisis largos sustituyen al probe.

## Estado pale-garden (el hueco grande: recall 61.46%)

Datos seed 424242, refs fresh de hoy:

| Chunk | vanilla bases | neutron bases | match |
|---|---|---|---|
| (0,0) | 37 | 51 | 10 |
| (1,0) | 31 | 31 | 6 |
| (0,1) | 44 | 41 | 14 |
| (3,3) | **0** | **7** | 0 |

- Gate 3×3 ✓ correcto (ChunkGenerator.java L329-335 usa rango cerrado ±1).
- Orden features/seeds ✓ (`feature_sorter_matches_vanilla_probe`).
- Conteos ≈ iguales salvo bordes → stream RNG sano; posiciones divergen dentro
  del mismo feature.

### Pista principal (chunk 3,3)

El chunk TIENE quarts pale_garden (probe `pale_gate_probe`), vanilla pasa el
gate igualmente, pero sus 16 intentos fallan todos y neutron coloca 7. La
divergencia está en el TERRENO bajo las posiciones candidatas (would_survive =
soporte del bloque inferior), no en gating ni RNG.

## Plan quirúrgico (siguiente sesión — ejecutado parcialmente hoy, ver abajo)

1. Trace neutron: para origin (3,3)·VEGETAL·pale_garden_vegetation, loggear las
   16 posiciones candidatas (x,y,z) + bloque debajo (NEUTRON_DECO_TRACE o probe nuevo).
2. Mismo listado desde ref vanilla: leer .mca chunk (3,3) y extraer bloque en
   (x,y-1,z) para cada candidata (y también buscar troncos pale reales cercanos).
3. Comparar: si vanilla tiene p.ej. `pale_moss_carpet` encima / `waterlogged` /
   y-distinta por heightmap OCEAN_FLOOR vs WG → ese es el bug concreto.
4. Fix + `lush_pale_parity` debe subir recall (objetivo ≥75% solo con árboles)
   y `region_parity` no bajar.

## ADDENDUM (misma sesión) — Crisis de medición descubierta y resuelta

Las refs del día tenían **1 solo chunk `full`** cada una (el de spawn): el
extractor esperaba Done + 90s, pero sin jugadores vanilla no progresa chunks.
Todas las medidas multi-chunk previas eran artefactos.

### Fix
1. Ejemplos de medida filtran `Status == *full` (lush_pale_parity,
   region_parity, block_parity, clay_overlap, pale_trunk_count); los stubs
   ahora son `missing`/skip en vez de comparar contra aire.
2. `ref-extract` nuevo flag `--forceload "X1 Z1 X2 Z2"` (+ `--gen-wait-secs`):
   tras Done envía `forceload add` por stdin → zona objetivo llega a full.
   Verificado: seed 12345 → 247 full; seed 424242 → 400 full (antes 1 y 1).

### Números honestos (refs con forceload)

| Métrica | Antes (contaminado) | Ahora (honesto) |
|---|---|---|
| region_parity 12345 (6,-2) 3×3 | "97.84%" (8 stubs) | **98.23% sobre 9 chunks reales** (rango 97.28–98.92) |
| chunk único (6,-2) | 98.12% | 98.13% ✓ consistente |
| lush_pale recall (424242, área pale) | "61.46%" mezcla stubs | **59.95%** sobre 22402 celdas vanilla reales |

Missing por bloque (área pale, honesto): pale_oak_leaves 2315 · clay 2055 ·
pale_oak_log 1266 · moss_block 1017 · pale_hanging_moss 589 · cave_vines 656 ·
resto ~700. Totales vanilla/neutron muy cercanos (22402 vs 22606) → el problema
es POSICIÓN/selección, no volumen global.

### Siguiente (quirúrgico, refs ya válidas)
1. deco_stream_probe sobre chunk full del área pale (ya no hay excusa de stubs):
   diff superficie vanilla-vs-neutron columna a columna → localizar si es
   heightmap OCEAN_FLOOR vs WG o un bloque de soporte mal clasificado.
2. Clay 2055: cruzar con gate LUSH_CAVES por quart real (storage 4×4×4 ya existe).
