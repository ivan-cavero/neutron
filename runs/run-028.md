# Run 028 — BiomeFilter en ores + andesite_upper vs región vanilla

> **Fecha**: 14 ago 2026  
> **Bar 1:1**: 100% block match — **aún no**  
> **Métricas (6,-2)**: ALL **97.02%** · BASE **99.00%** (igual que R27)

## 1. Hechos nuevos (vanilla 26.2)

`ProbeFeatureOrder` contra `server-26.2.jar`:

```
step 6 index 6 = minecraft:ore_andesite_upper
```

Scan de `r.0.-1.mca` (414 chunks):

- andesite **Y≥64**: 6365 bloques en **28 chunks** (p.ej. (5,-2)=196, (6,-1)=423, (5,-3)=593)
- chunk **(6,-2): 0** andesite alto
- andesite Y<64: 101274

Vanilla **sí** corre `andesite_upper`. En este chunk no escribió el blob.

Origen del extra Neutron: `(105, 98, -26)`, bioma Neutron = **lush_caves (id 34)** — el datapack incluye `ore_andesite_upper`.  
RNG (rarity/xz/y/ángulo/sin/nextDouble) = jar. `doPlace` Java = 634 bloques en (6,-2).

No hay fix 1:1 sin ver `ensureCanWrite` / `getHeight` en un generate vivo de ese chunk.

## 2. Cambios de código (vanilla, no heurística)

1. **`BiomeFilter.hasFeature`** en step 6: además del allow-list, se exige que el placed id esté en `features[6]` del bioma en el origen.
2. **`validTreePos`**: `isAir || REPLACEABLE_BY_TREES` — **agua fuera** (CFR).

En (6,-2) no mueven el %. lush_caves tiene andesite_upper; no hay agua en las copas.

## 3. Tests

`cargo test -p neutron-worldgen --lib` → 40 passed.

## Resultado

FAIL (bar 1:1). Sin movimiento de % en el chunk bar. Andesite extra sigue siendo **un blob upper** que vanilla no escribió aquí.

## Siguiente

Generate vivo 26.2 de (6,-2), o ChargeCursor / TreeFeature count (469 hojas).
