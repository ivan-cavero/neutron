# Run 034 — ProbeSculkPatch: ChargeCursor 1:1 hasta el tick 2, muerte en el 3

> **Fecha**: 14 ago 2026  
> **Bar 1:1**: 100% block match — **aún no**

## 1. Probe vanilla (Bootstrap + SculkSpreader real)

`tools/java-probe/src/ProbeSculkPatch.java`: suelo plano y=9 deepslate, origen (8,10,8), seed 1, 10×32, 64 attempts.

| Después | Vanilla | Neutron |
|---------|---------|---------|
| tick 1 | 10 cur, 1 sculk, 8 vena, **175 bits** | **igual** |
| tick 2 (delay) | igual | **igual** |
| tick 3 | **10** cur, 4 sculk, 16 vena, 402 bits | **3** cur, 4 sculk, 16 vena, 241 bits |
| final | 166 sculk, 174 vena, roll=0.821 | 92 sculk, 110 vena, roll=0.334 |

Posiciones y `faces=[down]` tras tick 1: **idénticas**.

En tick 3 los 3 primeros cursores coinciden (`8,10,7 ch=30`, `8,10,10 ch=31`, `9,10,7 ch=31`) y dejan `(7,10,8)` / `(8,10,9)` en **Air**. Vanilla reanima a los 7 que llegan a Air; nosotros los matamos (`DEFAULT` + `decay=0` → charge 0, sin `getValidMovement`).

## 2. Fix javap aplicado

`SculkBehaviour$1.attemptSpreadVein`:

- `facings == null` → sameSpace
- `facings.isEmpty()` → regrow
- else → veinSpreader.spreadAll

Teníamos empty/non-empty **al revés**. Corregido. En el suelo plano **no cambia** el recuento (el Air+DOWN no reaparece como vena: `stateCanBeReplaced` rechaza against=SCULK). Vanilla aun así mantiene 10 cursores — queda el mecanismo que les da `charge>0` en Air.

## 3. Métricas chunk (6,-2)

`cargo test -p neutron-worldgen --lib --release` → **46 passed**.

```
sculk_count: final_sculk=382 (era 330) vein=182 catalyst=0  starts=11
ALL blocks:  match 96736/98304 (98.40%)   [era 98.41%]
BASE:        match 95877/96173 (99.69%)
```

Más sculk (382/518) pero también 137 `air→vein` extra. El swap es javap-correcto; el residual del tick 3 sigue matando cursores y desfasando el roll.

## Resultado

FAIL (bar 1:1). Gap localizado al **tick 3** de ChargeCursor, reproducible contra el jar.

## Gaps a R35

1. Por qué vanilla no mata los 7 cursores en Air+`faces=DOWN`+`decay=0` (mismo mundo 4 sculk / 16 venas). Eso alinea bits y el catalyst roll.
2. Dark oak / BASE residual
