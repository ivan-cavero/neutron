# Run 015 — F2d R9: CaveWorldCarver bit-exact + re-diagnóstico del residual

> **Fecha**: 9 ago 2026  
> **Bar 1:1**: 100% block match — **aún no**  
> **Resultado**: carvers portados con Mth.sin table + LegacyRandom; residual de shape **no se mueve** en chunk (6,-2) porque **0 writes** de carver (RNG idéntico a Java)

## 1. Port CaveWorldCarver (esta ronda)

| Pieza | Estado |
|-------|--------|
| `LegacyRandom` + `setLargeFeatureSeed` | ✅ match Java bit-a-bit |
| `isStartChunk` / caveCount / thickness | ✅ |
| `Mth.sin`/`Mth.cos` tabla 65536 | ✅ (antes `f32::sin`) |
| `createRoom` / `createTunnel` / fork | ✅ bytecode-aligned |
| `canReach` / `carveEllipsoid` / yd=`y-0.5-cy` | ✅ |
| `CARVERS_ENABLED` | **true** |
| CanyonWorldCarver | ❌ pendiente |
| Aquifer `getCarveState` | ❌ simplificado (air/lava only) |

## 2. Evidencia RNG Java ≡ Rust

```
java (6,-1) nextFloat=0.12777710
rust (6,-1) nextFloat=0.12777710
total_starts=67 (ambos)
nearest starts caveCount=0 (ambos): (6,-1), (5,-3)
```

## 3. Writes por target (sólido stone, seed 12345)

| Target | writes |
|--------|--------|
| **(6,-2)** | **0** |
| (5,-2) | 23 |
| (6,-3) | 498 |
| (6,-5) | 393 |
| (3,-2) | 2063 |
| (0,0) | 1841 |

Para el chunk de paridad: **67 starts → 1990 ellipsoids → 1918 early-out + 72 empty-range → 0 hits**.

## 4. Density-phase shape (sin cambio vs R8)

| Métrica | Carvers OFF | Carvers ON |
|---------|-------------|------------|
| density_shape | 97492/98304 (99.1740%) | **igual** |
| neu_extra_solid | 746 | 746 |
| pure minecraft:air gaps | 169 | 169 |
| sculk (contado density-air) | 536 | 536 |
| sculk_vein/sensor/etc | ~41 | ~41 |

**Conclusión:** en (6,-2) los carvers clásicos **no afectan** la métrica. El residual **no** se cierra solo con CaveWorldCarver en este chunk.

## 5. Reclasificación del residual (746 “extra solid”)

Desglose vanilla donde Neutron es sólido:

| Vanilla block | count | Interpretación |
|---------------|------:|----------------|
| sculk (+vein/sensor/…) | ~577 | feature deep_dark, **no** density/carver phase |
| pure air / cave_air | **169** | aberturas reales; density SinglePoint/Java+Neutron = **solid** |
| moss/grass feature | ~5 | features |

Componente open grande en vanilla: **size=1405** Y=[-47..-12] (air+sculk flood-fill).

Density en muestras de ese open (Java ProbeNoodle + Neutron gen_fd): **solid** (p.ej. 0.0258 en 96,-47,-20).

## 6. Implicaciones

1. **Hipótesis R8 incompleta:** “746 = solo classic carvers” no cuadra con **0 carver writes** y RNG idéntico.  
2. Posibles fuentes del open de 1405 celdas:
   - **NoiseChunk real** ≠ SinglePoint/interp actual (caches/order) → density debería ser air y nosotros fallamos el fill.
   - Features/sculk que reemplazan sólido (menos probable para 954 air).
   - Carver geometry aún no bit-exact en paths lejanos (pero starts cercanos tienen caveCount=0 en **ambos**).
3. Sculk como “density-air” en `density_shape` **infla** el residual de forma (577/746).

## 7. Siguiente bar (R10)

1. **Métrica limpia:** reportar por separado `pure_air_gap` / `feature_fill_gap` / `fluid`.  
2. **Probe Java NoiseChunk doFill** (o dump de chunk pre-carver) en coords del open 1405 → ¿density air o solid en pipeline real?  
3. Si density real = air → fix interp/marker/aquifer en fill.  
4. Si density real = solid → instrumentar `applyCarvers` vanilla contando writes en (6,-2).  
5. Port **CanyonWorldCarver** (index 2) para otros Y.  
6. Features sculk cuando density+carver estén cerrados.

## 8. Comandos

```bash
cargo run -p neutron-worldgen --example density_shape --release
cargo run -p neutron-worldgen --example carver_many --release
cargo run -p neutron-worldgen --example gap_blocks --release
# Java
java -cp ... ProbeCarverCount
java -cp ... ProbeNoodle
```
