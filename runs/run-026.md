# Run 026 — OreFeature.place gate + canPlaceOre + Mth.sin

> **Fecha**: 14 ago 2026  
> **Bar 1:1**: 100% block match — **aún no**  
> **Sobre R25** (WorldgenRandom ya bit-exact)

## 1. Hallazgos (vanilla 26.2 javap OreFeature + Feature)

1. **`OreFeature.place` heightmap gate** — después de `nextFloat` + 2×`nextInt(3)`, **antes** de `doPlace`. Si ningún column del bbox cumple `minY <= getHeight(OCEAN_FLOOR_WG)`, no se llama `doPlace` (no se consumen los `nextDouble` del blob). Neutron siempre hacía `doPlace` → overplace de andesite/granite upper.

2. **`canPlaceOre` / `shouldSkipAirCheck`** — `nextFloat` se consume **siempre** si `0 < discard < 1`, **antes** de `isAdjacentToAir`. Neutron solo sorteaba si el bloque estaba expuesto → desync del resto del blob (coal buried 0.5, etc.).

3. **`isAdjacentToAir`** = `BlockState.isAir()` en 6 caras. Neutron contaba fluidos como aire.

4. **Radio del blob** usa `Mth.sin` (tabla 65536), no `Math.sin`. `t = (float)i/(float)size`.

## 2. Qué se hizo

- Gate OCEAN_FLOOR_WG en `place_ore_blob`
- `can_place_ore` alineado a CFR (RNG + isAir)
- Radio con `carvers::mth_sin_d`
- `placement_count` ya no muestrea dos veces (RNG)
- `surface_water_depth_filter` = WORLD_SURFACE − OCEAN_FLOOR (este chunk no se movió)

## 3. Métricas chunk (6,-2) seed 12345

| Métrica | R25 | **R26** |
|---------|----:|--------:|
| dens_shape | 99.5534% | 99.5534% |
| **ALL block match** | 93.89% | **96.96%** |
| **BASE (no veg)** | 95.85% | **99.00%** |
| diorite | — | **1046 = vanilla** |
| andesite neu | 2051 | 1969 (van 1424) |
| clay | 745 | 696 (van 703) |

```
ALL blocks:    match 95320/98304 (96.96%)
BASE (no veg): match 94811/95766 (99.00%)
DENSITY-PHASE: 97865/98304 (99.5534%)
```

Top mismatches: stone→andesite 545 · air→dark_oak_leaves 516 · sculk→deepslate 311 · air→dark_oak_log 246.

Gravel/dirt/coal salieron del top. Diorite exacto.

## 4. Tests

`cargo test -p neutron-worldgen --lib` → 40 passed (incluye `should_skip_air_check_matches_vanilla_consumption`).

## Critic (ciego)

**PASS** (bar de ronda). 40/40 tests; `block_parity` re-ejecutado 96.96% / BASE 99.00%.

## Resultado

FAIL (bar 1:1 global) — **BASE 99%**. Stall de ores residuales roto. Critic de ronda PASS.

## Gaps a R27

1. Extra andesite 545 (un blob upper que vanilla salta?)
2. TreeFeature extra dark_oak (516+246)
3. ChargeCursor 137/520
