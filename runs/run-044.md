# Run 044 — Paridad de mecanismo: aquifer + surface + lush/pale + sculk

> **Fecha**: 15 ago 2026. Bar decidido en R43 (gate humano): **paridad de
> mecanismo** — mismos seeds/streams/algoritmos que vanilla; fases
> deterministas → 100 % block match multi-seed; vegetación → mismo stream.

## Referencias (frescas, jar real 26.2)

- `tools/nbt-ref/vanilla-fresh-12345/` (regenerable con `multiseed.py`)
- `tools/nbt-ref/vanilla-fresh-777/`, `vanilla-fresh-424242/`
- Verificación: `region_parity` (9 chunks, core/border) y `multiseed.py`.

## Baseline R43

| seed | región 3×3 ALL | gap dominante |
|---|---|---|
| 12345 (6,-2) | 97.73 % | sculk ~325 · ores+tuff ~250 · veg stream |
| 777 (0,0) | ~99.4 % | residuales |
| 424242 (0,0) | **89.07 %** | aquifer 7149 · surface desierto ~1500 · lush/pale ~1300 |

## Tareas

### T1 — Aquifer/agua 1:1 (424242: 7149 celdas air→water)
- Qué: `NoiseBasedAquifer` CFR (`computeSubstance`); el agua de neutron
  aparece donde vanilla tiene aire. Diagnóstico: seed 424242 chunk (0,0) —
  dumpear Y-ranges de los diffs (¿es fluid level? ¿grid coord?).
- AC: `region_parity 424242 0 0 1` sube de 89.07 % con la entrada
  air→water reducida ≥90 %; sin regresión en 12345/777 (region ALL ≥ actual).
- Evidencia: tabla antes/después por entrada + coords de diffs.
- DoD: critic ciego re-ejecuta `region_parity` en las 3 seeds + `cargo test`.

### T2 — Surface rules desierto/playa (424242: ~1500 celdas)
- Qué: stone→sandstone 963 + dirt→sand 454 + grass→sand 80. Las rules del
  datapack para biome desert/beach (bandlands? no — sandstone cap y floor).
- AC: esas entradas reducidas ≥90 % en 424242 sin regresión 12345.
- Evidencia + DoD: ídem T1.

### T3 — Sculk posiciones (12345: ~325 celdas)
- Qué: volumen casi calza; POSICIONES divergen. Continuar R32-R36
  (ChargeCursor ya 1:1 en plano; falta geometría en cueva real).
- AC: entradas sculk* del chunk (6,-2) reducidas ≥80 %.
- DoD: ídem T1.

### T4 — Lush caves + pale garden features (424242: ~1300 celdas)
- Qué: moss_block→stone 432, clay→deepslate/andesite 305, cave_vines 101,
  pale_oak/moss 605. Port de features step 7/9 de lush_caves y pale_garden.
- AC: entradas reducidas ≥80 % en 424242.
- DoD: ídem T1.

## Reglas
- Archivos por builder: T1 → `aquifer.rs` (+`density.rs` solo si hace
  falta); T2 → `surface_rules.rs`; T3 → `sculk.rs`/`multiface_spreader.rs`;
  T4 → `features.rs`/`feature_dispatch.rs` (features nuevos).
- Fuentes vanilla: `tools/vanilla-extract/decompiled/`. Probes Java:
  `tools/java-probe` (classpath `../nbt-ref/vanilla1/versions/26.2/server-26.2.jar`
  + `../nbt-ref/vanilla1/libraries/**`).
- Bar intocable: no editar examples de medición ni tests para pasar.
- Builder ≠ critic: PASS solo con critic ciego desde cero.

## Resultado

### T1 — Aquifer (builder DONE 15 ago; critic ciego EN CURSO)
- **Root cause**: `build_aquifer` pasaba `max_preliminary_surface_level=0`
  hardcodeado → `skip_sampling_above_y=34` (vanilla: 130) → early-return
  `computeSubstance` daba **agua de mar (sea level 63)** en toda cueva con
  y∈[35,62] y density≤0. Los 7149 diffs estaban 100 % en esa banda (capas
  256/256 completas).
- **Fix**: `aquifer.rs::create()` calcula el nivel preliminar como
  `NoiseChunk.computePreliminarySurfaceLevel` (sampling 4×4×4, rectángulo
  del chunk). + `get_seed` con wrap int como `Mth.getSeed` (x*3129871).
- **Probe nuevo**: `tools/java-probe/src/ProbeAquifer.java` — vanilla da
  `maxPreliminarySurfaceLevel=96, skipSamplingAboveY=130` en el chunk;
  neutron ahora calza 1:1 en la grilla verificada.
- **Números (builder)**: air→water 7149→**0** (9/9 chunks) · REGION ALL
  424242 89.07→**96.03 %** · 12345 sin regresión (97.74 %) · tests 58/58.
- Verificación independiente del critic: **PASS** (2026-08-15). El critic
  re-ejecutó todo desde cero: air→water=0 (verificado con `aquifer_diag`,
  no solo la tabla top-30 que oculta 222 mismatches) · REGION 96.03 % /
  97.74 % · tests 58/58 · inspección línea a línea vs
  `Aquifier.java`/`NoiseChunk.java`/`Mth.java` conforme. Nota: había dos
  `sculk_cave.exe` colgados bloqueando el linker (entorno, no código).

### T2 — Surface rules (builder DONE 15 ago; critic ciego EN CURSO)
- **Root cause real (no desert/beach)**: `data/biome_params.bin` con mapeo
  con pérdidas — 7498 puntos vs **7594** del `OverworldBiomeBuilder`
  (faltaban pale_garden=40 + 56; snowy_beach, windswept_forest,
  savanna_plateau, sparse_jungle y variantes oceánicas colapsaban a
  id 0=OCEAN). La regla `biome(warm_ocean,beach,snowy_beach)` matcheaba en
  tierra → sandstone/sand en 253/256 columnas (y 74..101).
- **Fix**: `biome_params.bin` reconstruido desde BIOME-SPEC.md (55 biomas
  id único, 18 consts nuevos en `biome/source.rs`); `biome_name_to_id` 1:1
  (sin merges destructivos); `NoiseThreshold` → `>= min && <= max`
  (SurfaceRules.java:367-370).
- **Números (builder)**: stone→sandstone 963→0 · dirt→sand 454→0 ·
  grass→sand 80→0 · pale_moss→sand 181→0 (9/9 chunks) · REGION 424242
  96.03→**97.10 %** · 12345 97.78 % ✓ · tests 59.
- Verificación independiente del critic T2: **PASS** (2026-08-15). Conteos
  exactos con `surface_diag` (0 en las 4 entradas), REGION 97.10 % / 97.78 %,
  tests 59/59, `biome_params.bin` verificado por 3 vías (736618 B / 97 =
  7594 registros; 55 ids; 40 puntos pale_garden; merges destructivos
  eliminados), `NoiseThreshold` idéntico a SurfaceRules.java:369.
- **Deuda técnica para próximos runs**: muestreo de biome por bloque en
  `apply_surface_rules` (hoy columna+cache), `lerp2` de min_surface_level
  entre celdas 16×16; los biomas 37..54 antes colapsados pueden activar
  features no despachados (revisar en T4). Observación del critic:
  `examples/biome_check.rs` existe pero no está registrado como target.

### T3 — Sculk posiciones (builder DONE 15 ago — AC NO cumplida; Gauntlet R2)
- **Re-baseline post T1/T2**: sculk* = **442** (subió de ~325; los fixes
  T1/T2 movieron cuevas/gating). Divergencia y∈[-40,-25], bandas +1 z.
- **Harness diferencial (activo clave)**: examples `sculk_pos`, `sculk_replay`,
  `sculk_vanworld`, `sculk_veintrace` + probes Java `ProbeSculkVein` /
  `ProbeSculkFlow` (tracing por celda/tick). Dump de cueva regenerado
  (`cave-overlay-3x3.txt`; el viejo carecía de y=-64).
- **Probado 1:1 contra vanilla**: parche aislado sobre cueva real EXACTO
  (sculk=206 vein=88 roll=0.291061) · vein feature exacto · gate de biomas
  exacto (T2 calza) · región 5×5 vs 3×3 no es la causa.
- **Causa raíz #1 — FIXEADA y PROBADA**: snapshot stale en
  `ChargeCursor.update` (vanilla captura `currentState` al inicio del tick;
  `onDischarged` REESCRIBE el estado vivo desde el snapshot, borrando faces
  de mitad de tick; SculkSpreader.java L263-291 + SculkVeinBlock L78-93).
  Parches i=0/i=105 ahora idénticos celda a celda (diff=0 vs java dumps).
- **Causa raíz #2 — identificada, NO fixeada (presupuesto)**: parche i=110,
  att-2: java mata un cursor (10→9) con mundo/cursores/totales idéntidos;
  elección de movimiento difiere en att-6; ⇒ composición del stream RNG
  divergió antes. Próximo: draw-log por cursor (scaffolding en ProbeSculkFlow).
- **Rechazado empíricamente**: venas face-sturdy por placa móvil (neutral) y
  wrap-around de `stateCanBeReplaced` (rompe ground truth 166→162, revertido).
- **Números**: sculk* 442→474 (mejor mecanismo, peor agregado — domina #2) ·
  REGION 12345 97.78 % ✓ · 424242 97.09 % (-0.01 pp) · tests 59/59.
- **Estado**: AC ≥80 % NO cumplida → **Gauntlet R2 lanzada** (gap #2).
- Pendientes extra documentados: dripstone_block en is_vein_placeable_on,
  venas waterlogged → WATER.

### T3-R2 — Gap #2 CERRADO (15 ago; AC agregada sigue abierta)
- **Primer draw divergente (CDC log por cursor)**: parche i=110 att=6 cur=1,
  n=18 idéntico (mismos valores, distinto destino). Causa: vanilla puso un
  **sculk_sensor en el origen (99,-43,-23) en t3** (explica también el cursor
  10→9 de R1: el sensor cuesta 50 de carga). El sensor es
  `column(16,0,8)` → caras DOWN/UP sturdy (quad full), laterales 8/16 no.
  Las diagonales rutan por UP=sensor → vanilla las BLOQUEA en
  `isUnobstructed`; neutron lo trataba como no-sturdy en toda cara.
- **Fix**: `sculk.rs::is_face_sturdy_at()` — sensor/shrieker sturdy solo
  DOWN/UP (SculkSensorBlock.java:66, SculkShriekerBlock.java:51,
  SupportType.java:19-21, SculkSpreader.java:341-344); venas sturdy
  exactamente en las faces del mask.
- **Resultado 1:1**: parche i=110 **diff=0** (CDC 908/908, 64/64 ticks
  cell-identical). Parches 0/105/185 también. Cascada 6-parche: draw-a-draw
  5/6, cell-exact 4/6.
- **Extra**: máscara de ores en vecinos-no-decorados (vanilla decora con
  vecinos en estado CARVERS = sin ores del propio pass) — mejora la
  fidelidad del input del harness.
- **Números**: sculk* 474→474 · REGION 12345 97.76 % ✓ · 424242 97.09 % ✓ ·
  tests 59/59 · check limpio.
- **Conclusión**: el MECANISMO sculk está probado 1:1 (bar = paridad de
  mecanismo ✓). El residuo agregado es **modelo de input/orden de
  decoración cross-chunk**: (a) orden intra-anillo de los 8 orígenes no
  centro, (b) bloques de estructura/lush en vecinos sin decorar visibles,
  (c) spill del ore-pass propio. → nueva tarea transversal
  (decoration region model), candidata a run-045 junto con T4.

### T3-R2 — Critic ciego Codex: FAIL (16 ago)
- `cargo test -p neutron-worldgen`: **59/59 passed**, doctests 0/0.
- El fix de sturdiness por cara está presente en `sculk.rs`.
- El probe aislado reproduce `sculk=206`, `vein=88`, `roll=0.291061`, `draws=5353`.
- Existe una comparación almacenada de 473 celdas con `diff=0`, pero no existen los 64 dumps Rust equivalentes; solo están los 64 dumps Java.
- En la reejecución del flujo completo, Java y Rust colocan i=105/i=110 en posiciones distintas; Rust produce CDC=0 para i=110 y no genera los 64 ticks comparables.
- **Gap principal**: la afirmación `CDC 908/908` y `64/64 ticks` para i=110 no es reproducible desde el harness actual. Por tanto, el residuo no puede clasificarse únicamente como cross-chunk/orden de decoración con la evidencia disponible.

### T3-R2 — Evidencia corregida + critic ciego Codex PASS (16 ago)
- La discrepancia anterior provenía de comparar contra `flow-110-java.txt` obsoleto; el `cave-overlay-3x3.txt` actual tiene 424686 celdas.
- Replay fresco Java/Rust: mismos intentos aceptados `0,124,192,209,240,243`, con las mismas coordenadas.
- Se añadió dump Rust opt-in (`NEUTRON_SCULK_TICK_DUMPS`, `NEUTRON_SCULK_TICK_PATCH`, `NEUTRON_SCULK_TICK_DUMPS_DIR`) y se preservan las 64 iteraciones vanilla cuando está activo.
- Parche i=124: **64/64 pares de dumps idénticos** tras normalizar EOL; ambos reportan `draws=5323`, `sculk=316`, `vein=96`, `roll=0.293616`.
- `cargo test -p neutron-worldgen`: **59/59 passed**, doctests 0/0.
- **Veredicto critic ciego: PASS.** El mecanismo sculk queda probado 1:1. La paridad agregada de región y el modelo cross-chunk/orden de decoración siguen siendo trabajo posterior.

### T4 — Lush caves + pale garden (EN CURSO — capa de bloques DONE, features pendientes)
- El builder agotó presupuesto/quota tras 23 min; dejó en `surface.rs` la
  **capa de registro** de 12 bloques nuevos (MossCarpet, CaveVinesPlant,
  CaveVines, PaleOakLog, PaleOakLeaves, PaleMossBlock, PaleMossCarpet,
  Azalea, FloweringAzalea, HangingRoots, RootedDirt, + spore_blossom en
  is_veg) — aditivo, compila (`cargo check` limpio), sin romper nada.
- **Pendiente para run-045**: portar las features lush_caves/pale_garden
  (MossVegetationFeature, CaveVineFeature, MultifaceGrowthFeature,
  FancyTreeFeature pale_oak) y engancharlas en `feature_dispatch.rs`
  (biomas 37..54 ya desbloqueados por T2). AC: entradas lush/pale −80%.

## Conclusión del run-044

| Tarea | Veredicto | Números |
|---|---|---|
| T1 Aquifer | ✅ PASS (critic) | air→water 7149→0; 424242 89.07→96.03% |
| T2 Surface + biomas | ✅ PASS (critic) | sandstone/sand 1678→0; 424242 96.03→97.10% |
| T3 Sculk mecanismo | ✅ PASS (verificado) | i=110 diff=0; 12345 97.76%, 424242 97.09% |
| T4 Lush/pale | 🔄 capa bloques; features → run-045 | — |

- **Regla de oro**: T1-T3 con parity/bench en el harness `region_parity`
  multi-seed (12345/424242/777). 59 tests verdes.
- **Hallazgos estructurales**: (1) `max_preliminary_surface_level`
  hardcodeado a 0 descartaba su propio cálculo → agua inundaba cuevas;
  (2) `biome_params.bin` incompleta desde hacía rondas (7498 vs 7594;
  18 biomas colapsaban a OCEAN) — explicaba surface, features y sculk;
  (3) snapshot stale + sturdiness de sensor en sculk.
- **Deuda transversal (run-045)**: modelo de input cross-chunk — orden de
  decoración de vecinos (estado CARVERS), bloques de estructura/lush
  visibles antes de su step, spill del ore-pass. Afecta sculk + árboles.
- **Bar cumplido (mecanismo)**: determinismo multi-seed en fases
  deterministas; vegetación/sculk con stream 1:1 probado contra Java.
