# Run 052 — Placement chain verificada exacta; desync = terreno (acuífero en cuevas)

> **Date**: 19 Aug 2026. LEAD: pi. Continuación de run-051.

## Verificado línea a línea vs vanilla decompilado (todo coincide)

- **`VegetationPatchFeature.place`** (clay_pool/clay_with_dripleaves → el mismo
  port `place_vegetation_patch`): `xz_radius` muestreado DOS veces (vanilla
  también — `xRadius = sample()+1; zRadius = sample()+1`), orden de los rolls
  (edge → scan → depth+extra_bottom), el loop de placeGround (`return i != 0`),
  el `surface.add(belowPos)`, y `distributeVegetation` (roll veg_chance en orden
  del HashSet). TODO coincide.
- **Tags** verificados contra el jar 26.2: `lush_ground_replaceable` =
  {#moss_replaceable, clay, gravel, sand} · `moss_replaceable` =
  {#base_stone_overworld, #cave_vines, #dirt, #mud, #moss_blocks, #grass_blocks}
  · `base_stone_overworld` = {stone, granite, diorite, andesite, tuff, deepslate}.
  Neutron coincide en los tres.
- **`isFaceSturdy` vs `blocks_motion`**: aproximación cercana (las diferencias
  son leaves/plantas — irrelevantes para el suelo de clay en cuevas).
- **JavaBlockPosSet**: replica el HashMap de Java (hash `(y+z*31)*31+x` con
  spread `h^(h>>16)`, rehash al 0.75, iteración por buckets) — correcto.

## El signal real (generate completo, chunk (0,0))

- clay Neutron **612** vs vanilla **435** (+41%) — y la distribución en Y lo
  localiza: banda y=0..16: **451 vs 252** (casi 2×); banda y=-16: **18 vs 84**
  (Neutron pone MENOS); y=64: 101 vs 87.
- El probe de placement sobre los refs (terreno final) agrega +53 — artefacto
  del terreno de placement-time (los refs ya tienen el clay/moss finales) — no
  es un signal limpio.
- Los 62 draws del clay: la traza muestra rejects (el scan exige aire inicial y
  suelo sólido ≤12 pasos — la mayoría de los draws caen en sólido/cielo).

## Conclusión

El exceso de clay (y trees/moss) está **conducido por el terreno** (base ~98%):
los accepts del env_scan dependen de los bolsillos de aire y los pisos de las
cuevas a y=0..16. Hipótesis concreta: **el acuífero de Neutron pone menos agua
en las cuevas de esa banda** → más pisos expuestos (suelo reemplazable) → más
draws aceptados → más clay. La banda y=-16 con MENOS clay (18 vs 84) apunta a
una frontera de agua/terreno distinta.

## Próximo paso (run-053)

- **T1**: comparar el agua/lava (acuífero) en las cuevas de y −16..64 entre
  Neutron y los refs (contar celdas de agua por banda en las 9 chunks; buscar
  dónde difiere el nivel). El acuífero de Neutron (`aquifer.rs`) es el
  sospechoso primario del desync de features de cueva.
- **T2**: tras el fix del acuífero, re-medir clay_overlap (192/435 → ~435/435)
  y recall (los trees también dependen de los rejects por agua).

## Evidencia cruda

- Clay por banda (centro (0,0)): `-16: 84/18 · 0: 252/451 · 16: 12/42 · 64: 87/101`
  (vanilla/neutron).
- Probe clay sobre refs: `before=435 after=488 placed=53` (artefacto).
- Traza: 62/62 draws del clay reject en el terreno final (scan exige aire).
- region_parity: BASE 97.7-98.5% · ALL 96.8-98.0% (features bajan ~1pp).
- Tests 242/242.
