# Run 035 — DEFAULT.regrow + canAttachTo(sculk): ChargeCursor 1:1 en suelo plano

> **Fecha**: 14 ago 2026  
> **Bar 1:1**: 100% block match — **aún no**

## 1. Lectura javap (ifne)

`SculkBehaviour$1.attemptSpreadVein`:

```
if (facings == null) sameSpace;
else if (facings.isEmpty()) super /* vein.spreadAll */;  // ifne → 80
else regrow(air/water);                                  // fall through
```

R34 había invertido empty/non-empty. Lo correcto (y el código original) es **no vacío → regrow**.

`SculkVeinBlock.regrow` usa `isValidStateForPlacement` / `canAttachTo`, **no** `stateCanBeReplaced`. Sculk es cubo lleno → `canAttachTo=true`. Así un cursor en Air con `faces=DOWN` sobre sculk **vuelve a colocar la vena** y sigue vivo.

`is_sturdy_attach` excluía Sculk/catalyst → el regrow fallaba y los 7 cursores del tick 3 morían.

## 2. ProbeSculkPatch (suelo plano, seed 1) — 1:1

| Tick | Vanilla = Neutron |
|------|-------------------|
| 1–2 | 10 cur, 1 sculk, 8 vena, 175 bits |
| 3 | **10** cur, 4 sculk, 16 vena, **402** bits |
| 16 | 10 / 41 / 70 / 1745 |
| 32 | 8 / 103 / 126 / 3314 |
| 64 | 2 / 166 / 174 / **4734** |
| roll | **0.821367** |

Test: `flat_floor_matches_probe_sculk_patch`.

## 3. Chunk (6,-2) seed 12345

```
tries=2304 biome_ok=388 spread_ok=10 placed_ops=1662
final_sculk=643  van=518   catalyst=1 (van=2)
Y-32..-17: 213 / 278  (era 135; miss 87 vs 152)
Y-16..-1:  106 / 38   overshoot
Y 0..15:    96 / 0    start extra (103,-2,-23)
```

Primer parche sigue en `(98,-43,-23)` i=0 pero `catalyst_roll=0.680` (vanilla sí coloca). Draws=6504. El stream del primer parche **en la cueva real** aún no es 1:1 (el 3×3×3 sí; el frente de la cueva / pre-sculk no). Por eso no sale `(103,-26,-31)` y aparece un start en Y=-2.

```
ALL blocks:    match 96680/98304 (98.35%)
BASE (no veg): match 95718/96011 (99.69%)
```

`cargo test -p neutron-worldgen --lib --release` → **47 passed** (46 + flat probe).

## Resultado

FAIL (bar 1:1). ChargeCursor **cerrado al jar en suelo plano**. Residual: primer parche en geometría real (roll 0.680) + overshoot 643/518.

## Gaps a R36

1. Por qué el `run_patch` i=0 en `(98,-43,-23)` consume 6504 bits y roll 0.680 si el algoritmo ya es 1:1 en plano (pre-sculk / wrap / 3D).
2. Dark oak 240 leaves
3. BASE residual
