# Run 038 — MineshaftStructure: start (4,-1) y primeras 4 piezas XZ 1:1

> **Fecha**: 14 ago 2026  
> **Bar 1:1**: 100% block match — **aún no**

## 1. Qué se portó (javap 26.2 + datapack)

- `structure_set/mineshafts.json`: `random_spread` spacing=1, `frequency=0.004`, `legacy_type_3`
- `legacy_type_3` = `legacyProbabilityReducerWithDouble`: LegacyRandom + `setLargeFeatureSeed(seed, cx, cz)` + `nextDouble() < 0.004`
- `GenerationContext.makeRandom`: mismo `setLargeFeatureSeed`; `findGenerationPoint` hace `nextDouble()` y lo tira
- `MineShaftRoom` BB: `(cx*16+2, 50, cz*16+2)` + `nextInt(6)` en X/Y/Z
- `createRandomShaftPiece` / `generateAndAddPiece` / Corridor / Crossing / Stairs `find*`
- `moveBelowSeaLevel(63, -64, rng, 10)`
- Piezas se aplican **después de carvers, antes de ores**

Start vanilla en chunk **(4,-1)** (NBT `structures.References` de (6,-2) = `ChunkPos(4,-1)`).  
Test `seed_12345_has_start_at_4_minus_1` **PASS**.

## 2. Árbol vs vanilla NBT (`dump_ms` en (4,-1), 121 children)

| # | Vanilla XZ | Neutron XZ |
|---|---|---|
| 0 room | 66..75, -14..-5 | **igual** |
| 1 stairs | 73..75, -23..-15 | **igual** |
| 2 corridor | 73..75, -43..-24 | **igual** |
| 3 crossing | 76..80, -44..-40 | **igual** |
| 4 | corridor **norte** 77..79, -59..-45 | corridor **este** 81..95, -43..-41 |

Y del room: vanilla `-44..-39`, nosotros `-34..-29` (**+10**). Causa: 133 piezas vs 121 → `nextInt` de `moveBelowSeaLevel` distinto.

El roll del primer parche **sigue 0.996056 / 5650**. Abrir el aire vanilla de (5,-2)+(5,-1) sigue moviéndolo a 0.30. Nuestras piezas no pisan esas 201 celdas.

## 3. Tests

```
cargo test -p neutron-worldgen --lib --release  → 50 passed
```

## Resultado

FAIL (bar 1:1). Frequency + 4 primeras piezas XZ = vanilla. Gap: `Crossing.addChildren` / colisión en la pieza 5 (EAST nos sale, vanilla sigue NORTH) y el Y del `moveBelowSeaLevel`.

## Gaps a R39

1. Alinear el árbol a partir de la pieza 4 (javap Crossing + Corridor.addChildren vs NBT)
2. Con Y 1:1, el aire en r=15 debería mover el catalyst_roll
3. postProcess completo (raíles, cobweb, fences) para 1:1 de bloques
