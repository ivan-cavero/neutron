# Run 021 — Arquitectura 1:1 + sculk desde CFR (sin heurísticas)

> **Fecha**: 9 ago 2026  
> **Política**: solo vanilla (datapack + decompile). **Prohibido** wall-paint / expand / seed rescue.  
> **Bar**: 1:1 generation — en progreso

## 1. Política de trabajo

1. **JSON datapack** = números, listas de features por bioma, placement  
2. **Java decompilado (CFR)** = algoritmos  
3. **Herramienta de re-sync** para cada update de Mojang  
4. Nunca “aproximar” para subir dens_shape  

## 2. Herramientas nuevas

| Path | Uso |
|------|-----|
| `tools/vanilla-extract/extract-worldgen.ps1` | Extrae jar → data crate + CFR + javap |
| `tools/vanilla-extract/WORLDGEN-SYNC.md` | Manual: bioma nuevo / version bump |
| `tools/vanilla-extract/lib/cfr-0.152.jar` | Decompilador |
| `tools/vanilla-extract/decompiled/.../Sculk*.java` | Fuentes de verdad sculk |
| `crates/.../datapack_fs.rs` | Lee `src/data/worldgen/**` en runtime |
| `crates/.../feature_catalog.rs` | Índices `setFeatureSeed` desde biome JSON |

```powershell
pwsh tools/vanilla-extract/extract-worldgen.ps1 -Version 26.2
# Nueva versión / bioma:
pwsh tools/vanilla-extract/extract-worldgen.ps1 -Version 26.3 -ServerJar path\to\server.jar
```

## 3. Sculk reescrito (fiel)

- Config desde `configured_feature` / `placed_feature` JSON  
- Feature index desde `biome/deep_dark.json` step 7 (`feature_catalog`)  
- Face bits en venas (`FaceMap`) — `attemptPlaceSculk` exige `hasFace`  
- ChargeCursor.update alineado a CFR (updateDelay, decay, kill 15, NON_CORNER move)  
- **Eliminado**: charge_flood_walls, expand_sculk_ring, vertical seed rescue  

## 4. Datos en crate

Sincronizados bajo `crates/neutron-worldgen/src/data/`:

- `worldgen/biome/*.json` (66)  
- `worldgen/placed_feature/*.json` (262)  
- `worldgen/configured_feature/*.json` (226)  
- `tags/block/sculk_replaceable*.json`  

## 5. Tests

```
feature_catalog::deep_dark_sculk_indices — ok (vein=0, patch=1)
feature_catalog::sculk_patch_config_loads — ok (charge 10, amount 32, attempts 64)
```

## 6. Métricas (tras quitar heurísticas)

Medir con:

```bash
cargo run -p neutron-worldgen --example sculk_count --release
cargo run -p neutron-worldgen --example density_shape --release
```

Esperado: volume sculk **baja** vs R14 heurístico (correcto: priorizamos fidelidad).  
El residual sculk se cierra solo con MultifaceSpreader/ChargeCursor bit-exact + RNG/placement idénticos.

## 7. Checklist bioma nuevo

Ver `WORLDGEN-SYNC.md` § New biome checklist.

## 8. Siguiente

1. Port completo `MultifaceSpreader.spreadAll` / `spreadFromFaceTowardRandomDirection`  
2. `SculkBehaviour.DEFAULT.attemptSpreadVein` = `sameSpaceSpreader` real  
3. TreeFeature / RandomPatch desde CFR + JSON  
4. Dispatcher de features por `configured_feature.type`  
