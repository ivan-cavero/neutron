# Run 051 — Cave-biome root cause CORREGIDA (falso positivo); desync real = vegetation_patch

> **Date**: 19 Aug 2026. LEAD: pi. Continuación de run-050.
> Objetivo: fijar el cave-biome (run-050 lo señaló como root cause). Resultado:
> **la causa raíz de run-050 era un falso positivo** — el comparador de biomas
> comparaba el biome del grid almacenado (refs) contra el biome VORONOI de
> Neutron, que son dos conceptos distintos. La clasificación de biomas es
> CORRECTA.

## Verificado EXACTO (probes vanilla 26.2 contra refs de esta máquina)

1. **Voronoi biomes: 0 mismatches en 250 celdas** (grilla 25 columnas × 10 y,
   camino completo: voronoi quart → climate target → nearest-point). El filtro
   `minecraft:biome` de vanilla usa `level.getBiome(origin)` =
   `BiomeManager.getBiome` = VORONOI (verificado en
   `BiomeFilter.shouldPlace` + `BiomeManager.getBiome` decompilados). Neutron
   usa el mismo camino (`biome_id_at_block` → `voronoi_quart` → `find_biome`).
2. **Streams RNG EXACTOS**: `ProbeClayDraws` vs `FeatureRandom` — los 62 draws
   de `lush_caves_clay` (count + in_square + height) son IDÉNTICOS draw por
   draw (draw 1: x=5 z=15 y=170 … draw 62: x=10 z=4 y=167).
3. **EnvironmentScanPlacement**: la lógica de Neutron coincide con la vanilla
   (allowed → loop max_steps → target, sin RNG).
4. **Feature-list unions**: vanilla {deep_dark, lush_caves, pale_garden} ==
   Neutron (mismo set en el 3×3).
5. **`biome_id_to_name`** incluye lush_caves → el filtro biome resuelve bien.

## Lo que el run-050 había malentendido

- El "2.43% de celdas mismatched" era el grid almacenado (4×4×4 por sección,
  `fillBiomesFromNoise`, quart directo) vs el voronoi (per-block). El filtro de
  placement usa el VORONOI — que coincide 100%. El grid almacenado solo afecta
  la lista de features (union) — y el union también coincide.
- El depth/offset y el fitness: los targets de climate son idénticos
  (d=8150==8150 en las celdas "mismatched"); la tabla de 7594 puntos es
  idéntica; la fórmula fitness (suma de cuadrados, offset=0 en todos los
  puntos del overworld) coincide. No hay bug de biomas.

## El desync real (con evidencia)

- `lush_caves_clay` colocado por Neutron SOBRE el terreno vanilla (probe):
  agrega +53 celdas de clay al final vanilla (435 → 488) → Neutron acepta más
  draws o genera patches más grandes que vanilla. El generate completo: clay
  612 vs 435 vanilla (mismo patrón "counts ok, posiciones mal" que trees).
- Los sospechosos concretos están DENTRO de `place_vegetation_patch`
  (feature_dispatch.rs:1387):
  - `xz_radius` se muestrea DOS veces (xr, zr) — verificar si vanilla
    (`VegetationPatchFeature`) lo muestrea una vez (consumo RNG distinto).
  - El orden de iteración `JavaBlockPosSet` (fix 355c3d5) — re-verificar vs
    el `HashSet` de vanilla.
  - depth/extra_bottom/extra_edge chance y las reglas de ground placement.

## Archivos

- `tools/worldgen-probe/src/ProbeCaveGrid.java` — grid con el camino COMPLETO
  de vanilla (voronoi + target + biome).
- `tools/worldgen-probe/src/ProbeParamsDump.java` — dump del parameter list
  (7594 puntos, incluye offsets — todos 0 en overworld).
- `deco_stream_probe.rs` `NEUTRON_DECO_GRID=1` — mismo grid del lado Neutron.
- Comparación: `target/grid_van.txt` vs `target/grid_neu.txt` → 0 mismatches.

## Próximo paso (run-052)

- **T1**: diff línea a línea de `place_vegetation_patch` vs el vanilla
  `VegetationPatchFeature`/`WaterloggedVegetationPatchFeature` decompilado
  (tools/mc-decompiler/output/26.2/): xz_radius (1 vs 2 muestras), el orden
  del set, depth/chances. Verificar el consumo RNG con el stream del probe.
- **T2**: idem para los trees (pale_oak trunk/crown) — el mismo patrón.
- Medición: clay_overlap (192/435 → objetivo ~435/435) + recall.

## Evidencia cruda

- `biome cells: 13824 mismatches: 336 (2.43%)` — INVALID (grid vs voronoi).
- Comparación correcta (voronoi): `0 mismatches de 250`.
- Streams clay: draw 1..62 idénticos vanilla==neutron.
- Probe clay sobre refs: `before=435 after=488 placed=53` (extra).
- Generate full: clay 612 vs 435 · pale_oak_leaves 583 vs 383 · logs 252 vs 167.
