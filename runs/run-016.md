# Run 016 — F2d R10: Interpolate ALL density markers (noodle fix)

> **Fecha**: 9 ago 2026  
> **Bar 1:1**: 100% block match — **aún no**  
> **Resultado**: pure-air density gap **cerrado**; shape **99.42%**; residual = sculk features

## 1. Root cause (confirmado con Java)

| Path | (102,-41,-26) | Significado |
|------|---------------|-------------|
| SinglePoint `finalDensity` | +0.000335 solid | probe engañoso |
| `getInterpolatedNoiseValue` (NoiseChunk) | **−0.003135 air** | fill real |
| `getBaseColumn` | **air** | sin carvers |
| Neutron pre-R10 | solid (Deepslate) | bug |

Noodle JSON tiene **4** nodos `interpolated` (gate, thickness, ridge_a, ridge_b).  
Solo interpolábamos el de la rama A (squeeze); noodle se evaluaba por punto → túneles solid de más.

## 2. Fix

- `CellInterpRuntime` + `collect_interpolated` en `density.rs`
- Generator muestrea **todos** los Interpolated de `final_density` en la grilla 4×8
- Fill evalúa `compute(final_density)` con lerp Y→X→Z (orden vanilla)

## 3. Métricas chunk (6,-2) seed 12345

| Métrica | R9 (antes) | **R10** |
|---------|------------|---------|
| Density-phase shape | 99.1740% | **99.4222%** (97736/98304) |
| pure_air extra | 169 | **0** |
| feature_fill extra (sculk…) | 577 | **568** |
| neu_missing_solid | 52 | **0** |
| fluid_m | 14 | **0** |
| ALL block match | ~84.9% | **85.10%** |

## 4. Residual

Solo **568** celdas: Neutron solid donde vanilla tiene sculk/feature fill.  
Density+carver phase para forma “abierta” está alineada en pure air.  
Siguiente: sculk patches / vegetation / ores bit-exact / canyon.

## 5. Comandos

```bash
java -cp ... ProbeInterpDensity
java -cp ... ProbeBaseColumn
cargo run -p neutron-worldgen --example density_shape --release
cargo run -p neutron-worldgen --example block_parity --release
```
