# Run 058 — Romper el estancamiento: árboles + ports T4 + puzzle del agua en paralelo

> **Date**: 20 Aug 2026. LEAD: pi (main). 3 builders en worktrees aislados (branches
> run058-trees / run058-ports / run058-water), merge secuencial a main.
> **Origin**: run-057 cerró con el "water puzzle" abierto (5 runs sin avance: 053-057).
> El usuario pide avance real y delegación máxima. Este run ataca los 3 frentes en
> paralelo con tareas CERRADAS (lección run-049: nada de investigación abierta).

## Auditoría de estado (§2.5) — hecha por LEAD antes de lanzar nada

- `git status`: 1 archivo sucio (`Cargo.toml` con entrada `tmp_struct` a un ejemplo
  gitignored que **rompía `cargo test --workspace`** — mismo bug que 8249a67). Revertido
  a estado committed; working tree limpio. `origin/main == HEAD` (0/0).
- Tests: **242/242 verdes** tras el revert.
- Refs en disco: **SOLO `vanilla-fresh-424242`** (STATE.md afirma 3 — 12345/777 NO
  están en esta máquina). Y el ref 424242 es un **rectángulo 23×8 = 184 chunks**
  (x∈[-11,11], z∈[0,7]), NO 529 como afirma STATE.md. El chunk (0,0) está en el
  **borde -z** del área generada; su vecino (0,-1) NUNCA se generó → contexto de
  generación con blending potencial (hipótesis para el puzzle del agua).
- Diagnóstico chunk (0,0) 424242 (block_parity): ALL 97.02% · **BASE (sin veg)
  99.01%** — el terreno es casi perfecto. Gaps reales: `cave_air→deepslate` 162
  (cuevas que Neutron no abre), clay 612 vs 435 (+177), pale_oak_leaves 583 vs 383
  (+200, árboles MÁS grandes o más árboles), moss/vines/hanging_moss.
- Fuentes decompiladas 26.2 en disco: `tools/mc-decompiler/output/26.2/src/` (4.849
  .java). Jar: `tools/mc-decompiler/jars/server-26.2.jar`. Java 25 Temurin ✓.
- `vines` y `root_system` YA están portados (HANDLED) — la nota B4 de STATE.md es
  stale. El único no-op relevante para lush/pale es `multiface_growth` (glow_lichen,
  revertido por regresión -0.81pp por acoplamiento con el terreno).
- Beardifier: `NoiseChunk.java:404` reemplaza `BeardifierMarker` (constante 0.0) por
  el Beardifier REAL; la mineshaft del ref (0,0) usa terrain_adaptation NONE → no
  contribuye. El probe del run-057 usa Marker → si hay estructura con adaptación,
  el probe NO replica la densidad real.

## Objective

Avance medible en 3 frentes en paralelo: (T1) paridad de árboles pale_oak (39% del
gap de 8.572 celdas), (T2) ports T4 + whitelist 20→~5, (T3) resolver o acotar el
puzzle del agua/terreno (5 runs estancados). Todo mergeado a main y pusheado.

## Bar (sin cambios — R43 mechanism parity)

- REGION 424242 ≥97.28% · 12345 ≥97.75% · 777 sin regresión (ratchet) · recall
  lush/pale ≥80% (objetivo, no se espera alcanzar en este run) · clay ~497 ·
  `cargo test --workspace` verde · dispatch_coverage verde.
- No editar ejemplos de medición/tests para pasar el bar.

## Tasks

### T1 — Paridad de árboles pale_oak (Builder A, worktree `wt-trees`, branch `run058-trees`)
- What: Neutron pone 583 pale_oak_leaves vs 383 vanilla en chunk (0,0) 424242
  (+200) y más logs. Encontrar la causa y alinearlo con vanilla.
- Hipótesis (en orden): (a) más árboles aceptados (over-accept por terreno/stream),
  (b) forma del árbol (foliage radius/height, draws), (c) tipo de árbol (pale_oak
  vs pale_oak_moss/branches).
- Herramientas: `deco_stream_probe` (con NEUTRON_TRACE_TREES=1) vs trunks vanilla
  (`vanilla_trunks`), `block_parity` en (0,0),(0,1),(1,0), fuentes decompiladas
  (TreeFeature, PaleOakTreeGrower, foliage/trunk placers).
- AC: árboles por chunk dentro de ~5% del ref (leaves/logs) O root cause documentado
  con evidencia de que es acoplamiento de terreno (→ pasa a T3). Sin regresión en
  region %. Tests + dispatch_coverage verdes.
- Evidence: salida deco_stream_probe (draws), block_parity antes/después, recall.
- DoD: crítico ciego re-ejecuta block_parity + recall sobre el merge.

### T2 — Ports T4 + whitelist 20→~5 (Builder B, worktree `wt-ports`, branch `run058-ports`)
- What: portar los no-ops confesados: desert_well, ice_patch (disk@step4),
  ore_infested (ore@step7), speleothem_cluster×2, fossil×2, freeze_top_layer,
  ice_spike (spike), iceberg×2, lake_lava (lake), large_dripstone, monster_room,
  sulfur_pool (sequence), bamboo×2, geode, kelp. Whitelist 20→~5 (solo lo
  genuinamente no portable, con razón).
- Fuentes: decompiladas (feature/ + structure/). Medición por-port sobre el ref
  424242 (run-049: la mayoría NO afectan 424242 — confirmarlo y documentarlo).
- AC: dispatch_coverage verde (whitelist coherente), `cargo test --workspace` verde,
  region % 424242 sin regresión, conteos por-port medidos.
- Evidence: salida dispatch_coverage, tests, block counts por feature.
- DoD: crítico ciego re-ejecuta tests + dispatch_coverage + region_parity.

### T3 — Puzzle del agua/terreno (Builder C, worktree `wt-water`, branch `run058-water`)
- What: resolver O acotar por qué el ref tiene agua en celdas con densidad +0.0037
  (sólido) en Neutron Y en el probe de vanilla. Presupuesto: **3 rounds máximo**.
- Preguntas cerradas, en orden:
  1. ¿Qué bloque hay en (12,1,15) del ref? ¿Dónde está el agua de (0,0) (bandas y,
     borde vs interior)? ¿Correlaciona con los 162 cave_air→deepslate?
  2. Contexto del ref: rectángulo 23×8, (0,0) en borde -z sin vecino (0,-1) →
     ¿blending en columnas z local 0..3? (comparar columnas borde vs interior).
  3. Fidelidad del probe: BeardifierMarker vs Beardifier real (NoiseChunk.java:404),
     cell grid (cellCountXZ), blending (el probe no tiene blending).
  4. Densidad de Neutron en TODAS las celdas de agua del ref (no solo (12,1,15)):
     ¿>0 en todas (bug de densidad/terreno) o <0 en algunas (bug de colocación)?
  5. Carvers: ¿los 162 cave_air→deepslate están en la misma banda y que el agua?
- AC: verdict con evidencia (artifact del ref / probe infiel / bug de densidad /
  bug de carver) + fix si lo hay. Sin tocar ejemplos de medición para maquillar.
- Evidence: dumps de bloques, comparaciones de densidad, logs.
- DoD: crítico ciego verifica el verdict contra el ref y el source decompilado.

### T4 — Provisionar refs 12345/777 (LEAD, background)
- Receta B2: `ref-extract extract --seed <S> --hash-mode blocks --output /tmp/refx-<S>/reference.json --servers-dir servers-ref --tmp-dir /tmp/refx-<S> --keep-tmp`, mover a `tools/nbt-ref/vanilla-fresh-<S>/`. `servers-ref/server-vanilla.jar` = copia/symlink del jar 26.2. Nota: el ref 424242 local es incompleto (184 chunks) — documentar.

## Evidence

- Auditoría: salidas git, block_parity (0,0), chunk counts, fuentes decompiladas.
- Por builder: salidas crudas con rutas y timestamps.
- Ratchet final: region_parity ×3 seeds + lush_pale_parity + clay_overlap + tests.

## Result

PENDIENTE (se rellena al cerrar).

## Rounds

- R1: T1/T2/T3 en paralelo (worktrees), T4 background.