# Run 049 — B4 parallel: full 9-chunk order (T3) ∥ remaining T4 ports

> **Date**: 19 Aug 2026. LEAD: pi, main checkout. Two parallel builders in
> native git worktrees (AGENTS.md §5.5): `neutron-wt-t3` (branch `b4-t3-order`)
> and `neutron-wt-t4` (branch `b4-t4-ports`). Critic after both merge.
> Continuation of run-048 B4 (vegetation gap closure). Baseline on main
> @ 8249a67: 424242 97.33% · 12345 97.80% · 777 98.54% · lush/pale recall
> 57.96% · clay 411/497 · tests 241/241.

## Objective

Close the lush/pale recall gap (57.96% → ≥80% bar) by (a) completing the
vanilla 9-chunk decoration order derivation (THE lever: trees = 39% of the
8,572 missing cells) and (b) porting the remaining confessed no-op features.

## Bar (unchanged, run-046 U5 / R43 — mechanism parity)

- Same seeds/streams/algorithms as vanilla. Deterministic phases → 100% block
  match multi-seed; vegetation/sculk → same RNG stream 1:1.
- **Ratchet**: no regression on ANY seed (424242/12345/777 region parity).
- lush/pale recall ≥80% (currently 57.96%); clay 424242 → ~497.
- `cargo test --workspace` green.
- Do not edit measurement examples/tests to pass.

## Tasks

### T1 — Full 9-chunk vanilla decoration order + water filter (worktree `neutron-wt-t3`, branch `b4-t3-order`)
- What: derive the vanilla chunk decoration order for the measured 3×3 around
  (0,0) seed 424242, implement it in `decoration_origin_order`, and apply the
  correct-OCEAN_FLOOR water filter fix TOGETHER (they are a package).
- Context (verified by previous session, workbench R5):
  - For chunk (0,0): vanilla before-neighbors = {(-1,0), (0,-1)} ONLY. Their
    pale_moss patches water-reject draws 1/4 → no tree RNG → stream aligned.
    The other 6 decorated AFTER (0,0) (patches absent at draw time).
  - `vanilla_spawn` order (implements just the center) measured WORSE
    (54.40%) — the other 8 chunks' orders are also wrong. Full order needed.
  - Water filter fix alone measured WORSE (-0.52pp) — must ship WITH the order.
  - Tool: `deco_stream_probe` runs the REAL neutron placement chain against
    the VANILLA terrain (refs on disk) → exact vanilla RNG consumption.
    `NEUTRON_TRACE_TREES=1`, `NEUTRON_DECO_TRUNKS=1` (vanilla trunk bases),
    `NEUTRON_DECO_SKIP_TREE_DRAWS=N` gates exist. The lost `tmp_order_probe`
    (gitignored) did the before-set search — rebuild it (gitignored) as needed.
  - Method: for each of the 9 chunks, determine its before-set by testing
    which neighbor-subset (features present vs stripped to carvers) reproduces
    the vanilla tree positions in that chunk. Combine into a consistent total
    order → implement in `decoration_origin_order`.
- AC:
  1. Full order implemented (all 9 chunks), not just the center.
  2. Order + water filter applied together; recall measured.
  3. Ratchet: 424242/12345/777 region parity no regression.
  4. Tests green (`cargo test -p neutron-worldgen`).
- Evidence: per-chunk before-set derivation logs, deco_stream_probe traces,
  region_parity ×3 + lush_pale_parity + clay_overlap outputs.
- DoD: critic re-runs the 3 parity examples + lush_pale + clay + tests from
  the worktree, checks the order is a real total order (not a hack).

### T2 — Remaining T4 ports (worktree `neutron-wt-t4`, branch `b4-t4-ports`)
- What: port the remaining KNOWN_NO_OP features in
  `tests/dispatch_coverage.rs` (whitelist 19 → ~5): desert_well, ice_spike,
  speleothem_cluster ×2, fossil ×2, freeze_top_layer, large_dripstone,
  monster_room, lake_lava, sulfur_pool, ice_patch (step-4 disk),
  ore_infested (step-7). Each: dispatch arm + whitelist entry removal +
  measurement.
- Context: `glow_lichen` port was MEASURED as a recall regression (-0.81pp)
  and reverted — cave-terrain coupling. Measure each port; revert any that
  regresses recall or region parity. Do NOT port `geode`/`bamboo`/`iceberg`
  (out of scope, documented).
- AC:
  1. Whitelist 19 → ~5 (only geode/bamboo/iceberg + reverted glow_lichen left).
  2. Ratchet: no region-parity regression on any seed; recall not worse.
  3. `cargo test --workspace` green (dispatch_coverage test must stay green).
- Evidence: per-port measurement logs (region_parity + lush_pale_parity),
  whitelist diff.
- DoD: critic runs the dispatch_coverage test + parity examples, audits the
  whitelist count and per-port measurements.

## Budget

2 rounds/task max, parallel, then LEAD merges both branches into main
(gauntlet-smoother if conflicts), then a blind critic on the merged result.

## Evidence
(Raw logs with timestamps — builders write to their worktrees, LEAD records.)

## Result
PENDING

## Rounds
- R0 (LEAD): audit §2.5 — main @ 8249a67 clean, refs verified on disk
  (worktree `neutron-wt-worldgen`, 529 chunks × 3 seeds), baseline re-measured
  live (424242 97.33% / 12345 97.80% / 777 98.54% / recall 57.96% / clay 411).
  Stale `tmp_order_probe` Cargo.toml entry removed (8249a67). Worktrees
  `neutron-wt-t3`/`neutron-wt-t4` created from main, refs symlinked.