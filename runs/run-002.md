# Run 002 — F1: Núcleo jugable

> **Fecha**: 8-9 ago 2026
> **Fase**: F1 — Núcleo jugable
> **Resultado**: ✅ PASS

## Objetivo
Protocolo 26.2 funcional + mundo Anvil + fuzz 1M inputs + E2E integration tests

## Bar
- [x] Protocolo 26.2: login/play packets, codec, tipos
- [x] Mundo Anvil: .mca read/write, level.dat, session.lock, directory structure
- [x] Fuzz: 1M random inputs → 0 panics
- [x] E2E: 93 tests integrados, 0 fallos

## Tareas ejecutadas

### T1 — Protocolo 26.2 ✅
- **Crates**: `crates/neutron-protocol/`
- **Archivos**: lib, error, types, packet, codec, login, play
- **Packet types**: Handshake, LoginStart, EncryptionRequest/Response, SetCompression, LoginSuccess, KeepAlive, JoinGame, ServerData, ChatMessage, SetDefaultSpawnPosition, SynchronizePlayerPosition, ChunkDataAndUpdateLight, BlockUpdate, SystemChatMessage (clientbound); KeepAliveResponse, PlayerPosition, PlayerPositionAndRotation, PlayerRotation, SetPlayerAbilities, ChatCommand, ClientStatus (serverbound)
- **Tests**: 47 unit + 7 integration
- **Decisión**: `#![forbid(unsafe_code)]`, VarInt/VarLong codec, bytes::Bytes zero-copy

### T2 — Mundo Anvil ✅
- **Crates**: `crates/neutron-world/`
- **Archivos**: lib, error, region, nbt, level, session, world
- **Funcionalidad**: Anvil .mca read/write, level.dat (gzip NBT), session.lock (PID-based), vanilla directory layout
- **Tests**: 39 unit
- **Decisión**: `ussr-nbt` en vez de `simdnbt` (stable Rust)

### T3 — Fuzz ✅
- **1M inputs** aleatorios (0-64KB cada uno)
- **0 panics** después de fixes
- **Bugs encontrados y corregidos**:
  1. Missing bounds checks en todos los decode methods (types.rs, login.rs, play.rs)
  2. VarInt frame-size undercount en codec.rs y packet.rs
  3. data_length validation (VarInt negativo → overflow)
- **Performance**: 1.7M–2.4M ops/s decode, ~440K ops/s compressed

### T4 — E2E Integration ✅
- **7 integration tests**: world persistence, login flow, packet roundtrip all types, world directory structure, codec stress (10K frames), types through codec, region vanilla format
- **93 tests totales** en workspace, 0 warnings

## Estructura final del workspace

```
neutron/
├── Cargo.toml                    # Workspace root
├── crates/
│   ├── neutron-protocol/         # Protocolo 26.2
│   │   ├── src/                  # lib, error, types, packet, codec, login, play
│   │   ├── tests/                # integration.rs (7 tests)
│   │   └── fuzz/                 # fuzz_decode.rs, bench_decode.rs
│   └── neutron-world/            # Mundo Anvil
│       └── src/                  # lib, error, region, nbt, level, session, world
├── bench/                        # Benchmark harness (workspace separado)
└── ...
```

## Siguiente fase
F1 completada. Siguiente: F2 — Worldgen paridad 1:1
