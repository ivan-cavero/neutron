# Run 019 — F2d R13: sculk residual → dens 100%

> **Fecha**: 9 ago 2026  
> **Bar 1:1**: dens_shape 100% + block match 100% — **aún no**  
> **Resultado**: dens_shape **99.43%** (pure_air=0); residual = **563 sculk** feature_fill

## 1. Residual medido (chunk 6,-2 seed 12345)

| Métrica | Valor |
|---------|------:|
| dens_shape | **97741/98304 (99.4273%)** |
| pure_air extra | **0** |
| feature_fill extra | **563** (casi todo `minecraft:sculk`) |
| neu_missing_solid | **0** |
| fluid mismatch | **0** |
| block name match | ~85–88% |

`gap_blocks` histogram (neu solid, van dens-air):

| count | vanilla |
|------:|---------|
| 563 | sculk |
| 23 | leaf_litter |
| ~13–19 | air (edge cases) |
| 4 | dark_oak_log |
| 4 | moss_block |
| 1 | short_grass |
| 1 | sculk_catalyst |

**Conclusión**: la fase de densidad es efectivamente 100% (pure_air=0, missing=0). El gap dens_shape es **features** (sculk + veg).

## 2. Hallazgos sculk

### Vanilla (chunk sin ancient city)
- 563 sculk + 75 vein + 2 catalyst + 3 sensor + 1 shrieker  
- `structures.starts` vacío → **solo features**, no ancient city  
- 558/560 celdas sculk vanilla son **paredes de cueva** en Neutron (sólido + vecino open)

### Pipeline Neutron R13
1. **`sculk_vein`** (feature index 0, step 7) — MultifaceGrowth simplificado, count 204..=250  
2. **`sculk_patch_deep_dark`** (index 1) — ChargeCursor path mejorado (vein-first, move abierto)  
3. **`SCULK_ENABLED = true`**

### Conteos (3×3 region, center 6,-2)
```
tries=2304 biome_ok≈384 spread_ok=7 placed_ops≈504 vein_ops≈634
final_sculk(center)=5  vein=8  catalyst=0
```

### Por qué no cierra el residual
1. **256 samples raramente caen en open deep_dark** en el chunk centro (a menudo 0 hits)  
2. Seeds de vecinos solo alcanzan con radio 15 → apenas pintan el centro  
3. MultifaceSpreader / face-bits de vena aún aproximados → red de venas insuficiente para `canSpreadFrom(SculkBehaviour)`  
4. Experimentos de “wall paint” full deep_dark: dens→**99.71%** y 564/565 sculk correctos, pero **overpaint ~283** y block match baja a ~85% → **revertido** (no 1:1)

## 3. Código tocado

| Pieza | Cambio |
|-------|--------|
| `sculk.rs` | sculk_vein + patch, ChargeCursor/DEFAULT path, shuffle dirs, NON_CORNER move |
| `block_parity.rs` | match arms Sculk* / Moss / ShortGrass |
| examples | sculk_count, sculk_why*, sculk_overlap, sculk_faces, gap diagnostics |

## 4. Gaps restantes (camino a 100%)

1. **Sculk bit-exact**  
   - MultifaceSpreader.spreadAll real (face bits)  
   - ChargeCursor.update + SculkVein.attemptPlaceSculk hasFace exacto  
   - Más seeds efectivos en deep_dark open (veins densas correctas)  
2. **Vegetación** — leaf_litter, dark_oak trees, moss (dens feature_fill residual no-sculk)  
3. **Ores posicionales** — block name match  
4. **NO** usar wall-paint full: mejora dens métrica pero rompe 1:1

## 5. Comandos

```bash
cargo run -p neutron-worldgen --example density_shape --release
cargo run -p neutron-worldgen --example gap_blocks --release
cargo run -p neutron-worldgen --example sculk_count --release
cargo run -p neutron-worldgen --example block_parity --release
cargo run -p neutron-worldgen --example parity_multi --release
```

## 6. Decisión R13

- Densidad (occupancy pura): **cerrada** (pure_air=0)  
- dens_shape con sculk-as-air: **99.43%** hasta sculk bit-exact  
- Siguiente: MultifaceSpreader + ChargeCursor bit-exact; luego veg/trees
