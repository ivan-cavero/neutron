# Run 048 — Resume run-047 on new PC: A3 (bench perf) + B2/B3 (worldgen parity)

> **Date**: 18 Aug 2026. LEAD: pi, main checkout + 2 native git worktrees.
> **Origin**: run-047 RESUME BOUNDARY (PC switch, Windows → Linux). main contains ALL
> PASSED work (Track A: provisioning/history/compare; Track B: server review + fixes).
> This run re-provisions runtime data (jars, reference worlds) on this machine and
> finishes the pending units: A3, B2, B3.

## Objective

1. **A3 (benchmarks)**: `neutron-bench run` stops dying with exit 101 (azalea/bevy_log
   logger panic in the bot crate); benchmarks workspace builds/runs from the repo root
   via a gate wrapper; build + benchmark wall-clock measured on THIS machine with evidence.
2. **B2 (worldgen)**: fresh 26.2 reference worlds for seeds 424242/12345/777 re-extracted
   on this PC (runtime data does not travel); baseline re-measured (run-046 numbers were
   unverifiable — no references on disk).
3. **B3 (worldgen)**: isolate + fix the 777 regression; port MossVegetationFeature +
   CaveVineFeature → lush/pale recall ≥80%. Ratchet: no regression on any seed.

## Bar (verbatim, from run-047 + run-046 U5)

**Track A**:
1. `bench/` gone; everything in `tests/benchmarks/`.
2. `cargo test --workspace` green in root AND benchmarks workspace; no worldgen regression.
3. `neutron-bench servers download <type> <version>` (Mojang manifest / Paper API + local
   fallback, central managed dir, multi-version layout); `grep -r "bench/servers" tools/` = 0.
4. Every run writes a timestamped versioned report into `tests/benchmarks/results/history/`;
   `compare` works over the history.
5. Benchmarks workspace builds in release from the repo root (toolchain resolved);
   build time measured before/after; benchmark wall-clock budget documented.

**Track B** (run-046, human decision R43 — mechanism parity): same seeds/streams/algorithms
as vanilla → 100% block match multi-seed; do not edit measurement examples/tests to pass.
U5 AC: clay 424242 → ~497 (lush/pale missing ≤20%); border diffs ≥30% down on 12345;
REGION 424242 ≥97.28%, 12345 ≥97.75%; **777 no regression** (ratchet); tests 59/59.
Server: boots, joinable, real chunks, TPS stable.

## Budget

3 rounds/unit max, 2 tracks parallel (A3 ∥ B2), B3 after B2, 1 smoothing pass per track,
then merge+push. Stop: bar met, 2 rounds no improvement, or user says stop.

## Environment facts (this PC, Linux)

- **Java 25**: `JAVA_HOME=/home/ivan.cavero/.local/share/JetBrains/Toolbox/apps/air/jbr`
  (JBR 25.0.2 — a real Java 25). Verified against vanilla 26.2 jar before use; if the
  jar refuses JBR, install Temurin 25 (Adoptium) — user instruction.
- **Benchmarks workspace** = own nightly workspace (`tests/benchmarks/rust-toolchain.toml`
  pins nightly). Build ONLY from inside `tests/benchmarks/` (rustup resolves from cwd).
- **Vanilla 26.2 jar**: `cd tests/benchmarks && cargo run --release -p neutron-bench --
  servers download vanilla 26.2` → `servers/vanilla/26.2/server.jar` (multi-version).
- **ref-extract** (`tools/`, human-owned — DO NOT EDIT) still expects the OLD single-jar
  layout: `--servers-dir <dir>` where `<dir>/server-vanilla.jar` exists. Provision by
  copying/symlinking the multi-version jar into a dir named `server-vanilla.jar`.
- Reference worlds land in `tools/nbt-ref/vanilla-fresh-<seed>/` (gitignored).
- Baseline evidence file: `runs/run-048-evidence-baseline.txt` (LEAD-owned; builder
  writes evidence to its worktree, LEAD records).

## Tasks

### T1 — A3: benchmarks perf (worktree `wt-bench`, branch `run048-bench`)
- What: `neutron-bench run` completes without exit 101; root gate wrapper works from repo
  root; build/runtime measured on this machine.
- AC:
  1. `cargo test --workspace` green inside tests/benchmarks.
  2. Reproduce exit 101 (bevy_log logger panic in neutron-bot) → fixed; a real
     `neutron-bench run --server vanilla --size small --scenario join-storm --runs 1`
     reaches the benchmark (no 101) with the provisioned jar.
  3. Root gate: documented wrapper (cd into tests/benchmarks) runnable from repo root.
  4. Measured times (raw evidence): cold build, warm build, benchmark wall-clock budget.
- Evidence: build timestamps, `time` outputs, test output, run log without 101.
- DoD: critic rebuilds/reruns the binary, shows no 101, runs tests, audits measurements.

### T2 — B2: reference extraction + baseline (worktree `wt-worldgen`, branch `run048-worldgen`)
- What: fresh 26.2 references for 424242/12345/777 + baseline re-measure.
- AC:
  1. `tools/nbt-ref/vanilla-fresh-<seed>/` exists for all 3 seeds (fresh 26.2 jar, Java 25).
  2. Baseline measured with raw output: region_parity %, clay_overlap count (424242 ~497
     expected), lush_pale_parity recall, border diffs on 12345, 777 numbers.
  3. No code changes (extraction + measurement only); tools/ untouched.
- Evidence: extraction logs, ls of nbt-ref dirs, the 3 parity example outputs.
- DoD: critic re-runs at least one seed's measurement independently + verifies dirs.

### T3 — B3: worldgen 777 regression + lush/pale recall (worktree `wt-worldgen`, after B2)
- What: isolate + fix 777 regression; port MossVegetationFeature + CaveVineFeature.
- AC:
  1. Ratchet: no regression on 424242/12345/777 vs B2 baseline.
  2. REGION 424242 ≥97.28%, 12345 ≥97.75%; 777 ≥ B2 baseline + improving toward ~99.4%.
  3. Lush/pale recall ≥80% (moss_block + cave_vines features ported).
  4. Tests 59/59 green; `cargo test --workspace` green.
- Evidence: 3-seed example outputs before/after, test output.
- DoD: critic runs the 3 parity examples fresh + full test suite.

## Rounds
- R0 (LEAD audit): see workbench.md.

## Result
PENDING
## Result (so far)

**T1 A3 — PASS (blind critic 4d393ab6, 2 rounds incl. resume)**:
- 14/14 tests green in tests/benchmarks (incl. `init_logging_is_idempotent`).
- Real `neutron-bench run --server vanilla --size small --scenario join-storm --runs 1`:
  EXIT=0, server boots, 10/10 bots, results JSON+MD written. No exit 101.
- Fix (144b288): global logger initialized ONCE via OnceLock; `bevy_log::LogPlugin`
  disabled per bot App. Join timeouts raised for MC 26.2 config phase (a77dc86).
- Root gate (12de2d9 + smoothing): `./bench build|test|run` from repo root on
  nightly 1.100.0. Root workspace regression: 241/241, exit 0.
- Measured (this machine): cold build 12:45.65 (379% CPU), warm 21.31s, full
  join-storm run 3m25s; budget documented.
- Smoothed (A4, 5f06e9b9): 3 harmonization commits (help subcommand + sane
  failure modes in bench, warning fixes, README working-dir clarity). Bot crate
  warning-free. Merged to main (ff to cc59594).
- Residual risks: TPS-via-RCON reports spark=N/A / time_query=0.0 on vanilla
  (pre-existing limitation, vanilla has no spark); stale inert stash@{0} from
  commit splitting; join p50 variance (26s in builder's run vs 6.7s in critic's).

**T2 B2 — PASS (blind critic 0f2bc3d2)**: references extracted + baseline
verified. See runs/run-048-evidence-baseline.txt.
**T3 B3 — IN FLIGHT** (builder cc25f22d).
