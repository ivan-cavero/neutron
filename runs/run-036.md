# Run 036 — spreadAll snapshot: ChargeCursor 1:1 en la cueva (seed 1)

> **Fecha**: 14 ago 2026  
> **Bar 1:1**: 100% block match — **aún no**

## 1. Diagnóstico

Replay de la cueva pre-sculk (ores, r=20 alrededor de (98,-43,-23)) en Neutron y `ProbeSculkPatch` (mismo mapa, seed 1):

Tick 1 idéntico (5 venas + 1 sculk, 175 bits). Tick 3: Neutron colocaba vena extra `(97,-44,-21)#EAST`.

Causa: `spreadAll` de vanilla recibe el `BlockState` del origen y **no ve caras añadidas en la misma llamada**. Nosotros leíamos el `FaceMap` en vivo: `SamePosition` añadía SOUTH al origen y acto seguido `WrapAround` usaba esa cara.

## 2. Fix

Snapshot de bloque + máscara al entrar en `spread_all` / `spread_from_face_toward_random_direction`. `canSpreadFrom` / `hasFace` del origen usan esa foto.

Replay cueva seed 1 tras el fix = jar:

| | Vanilla | Neutron |
|--|--|--|
| tick 3 | 10 / 8 sculk / 12 vena / 398 | **igual** |
| tick 64 | 5 cur / 5352 bits | **igual** |
| final | 206 sculk, 88 vena, roll 0.29106164 | **206 / 88 / 0.291061 / 5353** |

Suelo plano sigue 1:1 (test `flat_floor_matches_probe_sculk_patch`).

## 3. Chunk (6,-2)

```
final_sculk=446  van=518   catalyst=2=2
ALL:  96813/98304 (98.48%)     [mejor ALL]
BASE: 95921/96213 (99.70%)
Y-48: 292/202   Y-32: 119/278   Y-16: 35/38
```

Primer parche i=0 sigue en `(98,-43,-23)` pero `catalyst_roll=0.996` (van sí). Los 2 catalysts salen en `(103,-41,-26)` y `(110,-37,-31)`, no en `(103,-26,-31)`. El generate real aplica `sculk_vein` antes y el pre-sculk no es 1:1 con vanilla (BASE 99.70%).

`cargo test -p neutron-worldgen --lib --release` → **47 passed**.

## Resultado

FAIL (bar 1:1). ChargeCursor **1:1 al jar** en plano y en la cueva aislada. Residual: pre-sculk / vena previa al parche i=0 + dark oak + ores.

## Gaps a R37

1. Mundo justo antes del parche i=0 (vena step 7 + residual ores/carvers) vs vanilla
2. Dark oak 240 leaves
3. BASE residual
