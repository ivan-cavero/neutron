# Run 032 — ChargeCursor: shuffle/NON_CORNER 1:1 + capa Y=-32

> **Fecha**: 14 ago 2026  
> **Bar 1:1**: 100% block match — **aún no**

## 1. Diagnóstico (datapack + javap + probe)

`sculk_count`: 11 parches, `final_sculk=330` vs van **518**. Histograma:

| Sección Y | Neutron | Vanilla |
|-----------|--------:|--------:|
| -48..-33 | 171 | 202 |
| **-32..-17** | **135** | **278** |
| -16..-1 | 24 | 38 |

152 misses en Y=-32..-17: 141 deepslate + 10 gravel + 1 tuff. **Todos biome deep_dark (31).** No es filtro de bioma.

11 orígenes de parche (centro primero). En esa capa solo `(107,-28,-30)` (below=Air).  
`ChargeCursor.update` mata el cursor si `!closerThan(Vec3i(ox, cursorY, oz), 15.0)` → **euclídea xz < 15**.

Desde (107,-30) a la esquina (96,-17): dist≈17 > 15. Esa losa no la cubre ese parche. Hace falta **otro start** más cerca; no aparece en nuestros 11.

`can_spread_from` no se come aires con pared: solo 2 `spread_fail` (aire 6-vecinos aire, vanilla también falla).

## 2. Probes 26.2 (bit-exact)

`ProbeShuffle` + `ProbeNonCorner`:

- `Util.shuffle` 18 elems seed 12345 = `[7,6,14,12,1,16,17,10,13,2,9,5,15,4,0,3,11,8]`
- `Direction.allShuffled` = WEST UP SOUTH DOWN EAST NORTH
- `betweenClosed(-1..1)` filtrado = **mismo orden** que `non_corner_neighbours`

Tests: `fisher_yates_18_matches_probe_seed`, `one_patch_on_flat_floor_converts_deepslate`.

`extra_rare_growths` leído del JSON (0, ConstantInt sin RNG) + bucle javap (offset nextInt(5)-2).

## 3. Métricas

Sin cambio de volumen (esperado: no se tocó el crecimiento).

```
ALL blocks:    match 96740/98304 (98.41%)
BASE (no veg): match 95911/96205 (99.69%)
tries=2304 biome_ok=379 spread_ok=11 placed_ops=1267
final_sculk=330 vein=175 catalyst=0
```

`cargo test -p neutron-worldgen --lib --release` → **46 passed**.

## Resultado

FAIL (bar 1:1). Shuffle/NON_CORNER cerrados al jar. Residual: **capa Y=-32..-17** (143 sculk) fuera del radio 15 del único parche de esa capa; catalyst 0 vs 2.

## Gaps a R33

1. Por qué vanilla tiene más starts en esa losa (mismo seed/índice 1) o ChargeCursor cubre más
2. Dark oak 240 extra leaves
3. BASE residual
