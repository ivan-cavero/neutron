# Run 046 — Modelo de input cross-chunk (WorldGenRegion)

> **Fecha**: 16 ago 2026. Bar (decisión humana R43): **paridad de mecanismo**
> — mismos seeds/streams/algoritmos que vanilla. Continuación de run-045
> (causa raíz del clay inflado aislada: spillover de los 9 chunks en
> `apply_step_region`).

## Referencias (frescas, jar real 26.2)
- `tools/nbt-ref/vanilla-fresh-424242/`, `-12345/`, `-777/`
- Verificación: `block_parity`, `region_parity` (core/border), `multiseed.py`,
  examples `clay_overlap`, `lush_pale_parity`.
- Fuentes vanilla: `tools/vanilla-extract/decompiled/.../ChunkGenerator.java`
  (applyBiomeDecoration L263-333), `WorldGenRegion.java`.

## Baseline (cierre run-045)
| seed | REGION 3×3 ALL | gap dominante |
|---|---|---|
| 12345 (6,-2) | 97.75 % | input-model (borde) + vegetación |
| 424242 (0,0) | **97.28 %** | clay 840 vs 497, lush/pale missing 50.42 % |
| 777 (0,0) | ~99.4 % | residuales |

## Tareas

### U1 — Build limpio (prerequisito)
- Qué: `clay_probe.rs:116` no compila (`sections` fuera de scope) y está
  registrado en Cargo.toml → `cargo test --workspace` falla.
- AC: `cargo test --workspace` exit 0; tests worldgen 59/59; sin regresión
  en examples registrados de medición.
- Evidencia: salida de cargo test (tail), git diff.
- DoD: critic ciego ejecuta `cargo test --workspace` desde cero.

### U5 — Modelo de input cross-chunk (arquitectura)
- Qué: replicar `WorldGenRegion` de vanilla — al decorar el chunk X (central),
  los vecinos están en estado CARVERS (terreno+cave+estructuras, SIN ores ni
  features del propio pass); el paso de features de X escribe en la región
  3×3 y los vecinos decorados después pueden sobrescribir el borde. Hoy
  `generate_chunk` es autónomo → el spillover de los 8 vecinos inunda el
  chunk central (clay 840 vs 497).
- AC: clay 840→~497 (missing lush/pale ≤20 %); border diffs ≥30 % down en
  12345, cores sin regresión; REGION 424242 ≥97.28 %, 12345 ≥97.75 %;
  tests 59/59.
- Evidencia: antes/después `region_parity` + `clay_overlap` + `multiseed.py`.
- DoD: critic ciego re-ejecuta mediciones desde cero.

## Reglas
- Archivos: U5 → `generator.rs` + `region_buf.rs` + scheduler de decoración
  (+ `feature_dispatch.rs` si el spillover vive ahí).
- Bar intocable: no editar examples/tests de medición.
- Builder ≠ critic; PASS solo con critic ciego desde cero.

## Resultado

(en curso — ver workbench.md)