# Run 046 — Cross-chunk input model (WorldGenRegion)

> **Date**: 16 Aug 2026. Bar (human decision R43): **mechanism parity** — same
> seeds/streams/algorithms as vanilla. Continuation of run-045 (inflated clay
> root cause isolated: 9-chunk spillover in `apply_step_region`).

## References (fresh, real 26.2 jar)

- `tools/nbt-ref/vanilla-fresh-424242/`, `-12345/`, `-777/`
- Verification: `block_parity`, `region_parity` (core/border), `multiseed.py`,
  examples `clay_overlap`, `lush_pale_parity`.
- Vanilla sources: `tools/vanilla-extract/decompiled/.../ChunkGenerator.java`
  (applyBiomeDecoration L263-333), `WorldGenRegion.java`.

## Baseline (run-045 close)

| seed | REGION 3×3 ALL | dominant gap |
| --- | --- | --- |
| 12345 (6,-2) | 97.75 % | input-model (border) + vegetation |
| 424242 (0,0) | **97.28 %** | clay 840 vs 497, lush/pale missing 50.42 % |
| 777 (0,0) | ~99.4 % | residuals |

## Tasks

### U1 — Clean build (prerequisite)

- What: `clay_probe.rs:116` does not compile (`sections` out of scope) and is
  registered in Cargo.toml → `cargo test --workspace` fails.
- AC: `cargo test --workspace` exit 0; worldgen tests 59/59; no regression in
  registered measurement examples.
- Evidence: cargo test output (tail), git diff.
- DoD: blind critic runs `cargo test --workspace` from scratch.

### U5 — Cross-chunk input model (architecture)

- What: replicate vanilla `WorldGenRegion` — when decorating chunk X, neighbors
  are in CARVERS state (terrain+caves+structures, NO ores or own-pass features);
  X's feature pass writes into the 3×3 region and later-decorated neighbors can
  overwrite the border. Today `generate_chunk` is autonomous → the 8-neighbor
  spillover floods the center chunk (clay 840 vs 497).
- AC: clay 840→~497 (lush/pale missing ≤20%); border diffs ≥30% down on 12345,
  cores no regression; REGION 424242 ≥97.28%, 12345 ≥97.75%; **777 no
  regression** (ratchet, added 16 Aug); tests 59/59.
- Evidence: before/after `region_parity` + `clay_overlap` + `multiseed.py`.
- DoD: blind critic re-runs measurements from scratch.

## Rules

- Files: U5 → `generator.rs` + `region_buf.rs` + decoration scheduler
  (+ `feature_dispatch.rs` if the spillover lives there).
- Bar untouchable: do not edit measurement examples/tests.
- Builder ≠ critic; PASS only with blind critic from scratch.

## Result

### U1 — Clean build (R1, 16 Aug) — ✅ PASS (blind critic)

- `cargo test --workspace` exit 0, worldgen 59/59, 25 measurement examples
  compile, `src/` untouched. run-045 WIP committed (0689ff8) after human gate.

### U5 — Cross-chunk input model (rounds)

| Round | Result | Numbers |
| --- | --- | --- |
| R1 | 🔍 investigation | Vanilla model verified (ChunkGenerator.java L263-341, WorldGenRegion.java, ChunkPyramid L20): origin-major, center first, neighbors in CARVERS, masking. Gaps C1-C8 + plan D. Builder pending |
| R2 | ❌ FAIL (blind critic) | clay 840→466 ✓; REGION 12345 97.94% ✓; but REGION 424242 97.20% (regression), border -7.5% (needs ≥30%), lush/pale recall 48.27%. Biggest gap: vegetation placement (pale oaks 3936, clay 2126, moss 1300 missing) → drags 424242. Fix: full FancyTreeFeature (2×2 trunk, canopy, branches) + lush_caves_clay placement chain → Builder R3 |
| R3 | 🔨 builder (working tree) | FancyTreeFeature pale oak + TreeDecorator sort + creaking_heart/pale_moss + lush_caves_clay placement chain. LEAD-measured: REGION 424242 **97.38%** ✓, 12345 **97.94%** ✓, clay **466** ✓, recall **62.94%** (bar ≥80 ❌), border -7.5% ❌, **777 96.29%** ❌ REGRESSION. Committed 91862d4. **Blind critic pending** |

## Status

**U5 R3 committed (91862d4); bar NOT met: recall 62.94% (<80%), border -7.5%
(<30%), 777 regression 96.29%. Next: isolate 777 regression (U5 vs R3 trees),
port MossVegetationFeature + CaveVineFeature, launch blind critic.**

*Last update: 16 Aug 2026 — R3 (LEAD-measured, see workbench.md)*
