# Run 047 — Dual-track: benchmarks refactor (Track A) + server review/worldgen (Track B)

> **Date**: 17 Aug 2026. LEAD: pi, main checkout + 2 Orca worktrees.
> **Session end**: interrupted by human (PC shutdown, resume on another PC) — see
> **RESUME BOUNDARY** at the bottom. Everything below marked PASS was blind-critic
> verified on this machine.

## Objective

1. **Track A (benchmarks)**: refactor + improve the benchmark harness — fast,
   reliable multi-version server provisioning, versioned report history, build/run
   that isn't slow or broken.
2. **Track B (server + worldgen)**: review the neutron server (test it properly) and
   continue world generation parity (run-046 continuation).

## Bar (verbatim)

**Track A**:
1. `bench/` gone; everything in `tests/benchmarks/`.
2. `cargo test --workspace` green in root AND benchmarks workspace; no worldgen regression.
3. `neutron-bench servers download <type> <version>` (Mojang manifest / Paper API + local
   fallback, central managed dir, multi-version layout); `grep -r "bench/servers" tools/` = 0.
4. Every run writes a timestamped versioned report into `tests/benchmarks/results/history/`;
   `compare` works over the history.
5. Benchmarks workspace builds in release from the repo root (toolchain resolved);
   build time measured before/after; benchmark wall-clock budget documented.

**Track B** (run-046, human decision R43 — mechanism parity): same seeds/streams/algorithms
as vanilla → 100% block match multi-seed; do not edit measurement examples/tests to pass.
U5 AC: clay 424242 → ~497 (lush/pale missing ≤20%); border diffs ≥30% down on 12345;
REGION 424242 ≥97.28%, 12345 ≥97.75%; 777 no regression (ratchet); tests 59/59.
Server: boots, joinable, real chunks, TPS stable.

## Budget

3 rounds/unit, 2 tracks parallel, 1 smoothing pass per track, merge+push incremental.
Stop: bar met, 2 rounds no improvement, or user says stop.

## Results

| Unit | Scope | Verdict | Evidence | Artifact (branch → merged to main) |
| --- | --- | --- | --- | --- |
| A1 | Multi-version provisioning (download/list/status, fallback, arch-aware pumpkin) | ✅ PASS (2 rounds) | critic re-downloads sha1/sha256 == Mojang/PaperMC API; pumpkin PE=AMD64; hostile inputs clean | 36c13a4, 3b7b0ff |
| A2 | Versioned report history + compare over history | ✅ PASS (3 rounds) | hand-verified deltas; missing/tie never win; 10 tests | c0876c6, 16fb597, 519bc2e |
| A3 | Perf: exit-101 panic fix, root gate, times | ⏳ IN FLIGHT (2 launches died: stream interrupt ×1, wall ×1) | — | — |
| B1 | Server review + smoke (boot/ping/join/TPS) + REVIEW.md | ✅ PASS (6/6) | Done marker 0.0s, ping valid, 21 chunks, TPS 20.00 no drift, 241/241 | 6444286, 89550c4, 7d45404, 2199b05 |
| B1b | Disconnect cleanup fix (critic-found defect) | ✅ PASS | RST → online:0 live; 241/241 | 272e30b |
| B2 | Reference extraction (fresh 26.2, 3 seeds) + baseline re-measure | ⏳ IN FLIGHT (no artifacts yet) | — | — |
| B3 | Worldgen: 777 regression + lush/pale recall ≥80% | ⏳ PENDING (needs B2 baseline) | — | — |

## Key environment facts (next PC)

- **Java 25** at `C:\Program Files\Eclipse Adoptium\jdk-25.0.4.7-hotspot` (installed via
  winget this session; vanilla 26.2 needs it). Other PC: install Temurin 25 if absent.
- Benchmarks workspace = own nightly workspace; build ONLY from inside `tests/benchmarks/`
  (rustup resolves toolchain from cwd). Cold azalea build ~9 min; warm ~9s.
- PaperMC v2 API is SUNSET — the downloader uses `fill.papermc.io/v3` (verified live).
- Vanilla 26.2 jar: download with `cd tests/benchmarks && cargo run --release -p neutron-bench -- servers download vanilla 26.2`.
- `tools/ref-extract` still resolves jars at the OLD path (`bench/servers/server-vanilla.jar`); tools/ is human-owned (do not edit; note already in tests/benchmarks/servers/README.md).
- Reference worlds live under `tools/nbt-ref/vanilla-fresh-<seed>/` (gitignored — do NOT travel via git; re-extract on the new PC, see B2).

## RESUME BOUNDARY (read first on the next PC)

1. `git fetch origin && git checkout main && git pull` — main contains ALL PASSED work
   (5 bench commits + 5 server commits merged, workbench history).
2. Reproduce the two worktrees (Orca) if desired: `orca worktree create --repo id:<repoId> --name bench-refactor --no-parent`, same for `server-worldgen`; check out branches
   `ivan-cavero/bench-refactor` / `ivan-cavero/server-worldgen` (pushed). Or work directly on main.
3. **Runtime data does NOT travel**: jars, reference worlds, target/ are gitignored.
   - Jars: `cd tests/benchmarks && cargo run --release -p neutron-bench -- servers download vanilla 26.2` (multi-version layout).
   - Reference worlds: re-run B2 extraction (fresh 26.2 jar + ref-extract, seeds 424242/12345/777 → `tools/nbt-ref/vanilla-fresh-<seed>/`).
4. **Next units**:
   - **A3** (bench-refactor branch): exit-101 azalea/bevy_log logger panic fix in bot crate; root gate wrapper (cd into tests/benchmarks); measure times (cold 8m52s recorded, warm ~9s). Then A4 smoothing.
   - **B2** (server-worldgen branch): extraction + baseline measurement (see unit brief in workbench.md round log) → baseline file `runs/run-047-evidence-baseline.txt`.
   - **B3**: worldgen — isolate 777 regression (96.29% vs ~99.4% claim — UNVERIFIED until B2), port MossVegetationFeature + CaveVineFeature for lush/pale recall ≥80%.
5. **Merge smoke test pending on this machine**: the two merges into main were disjoint-file and each branch was green (241/241 + bench tests) — run `cargo test --workspace` once on the merged main before further work.
6. Post-loop: smoothing pass per track, then final merge + push.

## Open questions for the human

- (none — loop interrupted by user request to stop and save state)